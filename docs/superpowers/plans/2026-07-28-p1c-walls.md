# P1c 벽면 파이프라인 + 경사 추종 구역화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스캔에서 수직 벽면을 자동 검출해 벽별 평활도(직선자)·수직도(plumbness)를 판정하는 `analyze_wall` 파이프라인을 추가하고, P1b 최종 리뷰 티켓(coverage_pct 편입·경사 추종 구역화·low_coverage 경고)을 해소한다.

**Architecture:** xy 컬럼 점유 그리드에서 z-범위가 큰 컬럼 = 벽 후보 → 2D 라인 RANSAC으로 벽 라인 추출 → 벽별로 점을 (u=벽방향, v=높이, w=법선편차) 로컬 좌표로 투영 → **기존 build_subcell_grid/evaluate_cells/grade_cells/heatmap을 그대로 재사용**(w가 z 역할) → 벽 평면 재피팅 잔차로 셀 판정 + dw/dv 기울기로 수직도. 바닥 구역화는 평면 추종 영역 성장으로 개정(경사 슬래브 커버리지 복원).

**Tech Stack:** 기존 engine 패키지 확장 (numpy, scipy.ndimage, laspy, matplotlib, pytest). 신규 모듈 `flatness/core/walls.py`.

**Spec:** `docs/superpowers/specs/2026-07-27-flatness-dashboard-design.md` §5.1.8(벽면), §5.1.3(구역화) · **백로그:** `2026-07-28-p1b-backlog-notes.md` 티켓 13·14·16

## Global Constraints

- **z-up 가정:** iPhone 내보내기 기준 z가 연직. 수직도는 z축 기준 **상대** 지표 — stats/CLI에 이 한계를 항상 표기(경고 `plumbness_relative_to_z` 고정 포함, 스펙 §5.1.8 중력 캐비앳)
- 수직도 판정: `plumb_mm = |dw/dv| × H × 1000` (H=벽 높이), 기준은 시드 `wall-kcs-plumb`(pass 25/rework 75, metric plumbness → s=1)로 **자동 병행 판정**. CLI `--criteria`는 metric=flatness만 허용(벽 셀 판정용)
- U 기본값: CLI `--uncertainty-mm` 미지정 시 바닥 5mm / 벽 8mm (스펙 §4.2)
- 셀 판정은 벽 평면 잔차(국부 요철), 전역 기울기는 수직도로 분리 — 바닥의 레벨 분리와 동일 원칙
- 벽 검출 파라미터: 컬럼 z-범위 ≥ 0.8m & 점 ≥ 3 → 벽 후보, 라인 RANSAC 인라이어 거리 < 5cm, 최소 길이 1.0m, 최대 8면, 투영 밴드 ±10cm
- 경사 추종 구역화: 초기 레벨 밴드 컴포넌트 확정 후 평면 추종 영역 성장(4-인접, |mz−plane|≤band, 최대 12회 반복) — 기존 two-rooms/유령/가구 테스트 회귀 금지
- low_coverage 경고: coverage_pct < 70 → warnings에 `low_coverage` (티켓 14)
- coverage_pct는 build_stats 파라미터로 편입(티켓 16) — 파이프라인 덮어쓰기 제거
- 사용자 출력 문자열에 U+2014(—)·cp949 밖 특수기호 금지
- 하위 호환: 기존 테스트 78개 무수정 통과(승인된 픽스처 조정 제외), 코드 주석 한국어, 커밋 트레일러 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, 테스트는 engine/에서 `python -m pytest`

---

### Task 1: coverage_pct의 build_stats 편입 (티켓 16)

**Files:**
- Modify: `engine/flatness/outputs/stats.py` (build_stats 시그니처), `engine/flatness/core/pipeline.py` (덮어쓰기 제거)
- Test: `engine/tests/test_stats.py` (테스트 추가)

**Interfaces:**
- Produces: `build_stats(cells, grades, crit, u_mm, warnings, meta, zones=None, coverage_pct=None)` — coverage_pct가 None이면 기존 셀 기반 계산(하위 호환), 주어지면 그 값을 반올림(round 1)해 사용. 후속 태스크(analyze_wall)와 pipeline이 이 파라미터를 사용

- [ ] **Step 1: 실패하는 테스트 작성** (`engine/tests/test_stats.py`에 추가)

```python
def test_coverage_pct_param_overrides_cell_based():
    # 티켓 16: 파이프라인 덮어쓰기 대신 파라미터로 편입 — 호출자별 의미 분화 방지
    crit = load_criteria()["floor-kcs-exposed"]
    cells = _cells()
    grades, warns = grade_cells(cells, crit, 5.0)
    s = build_stats(cells, grades, crit, 5.0, warns, {}, coverage_pct=87.34)
    assert s["coverage_pct"] == 87.3
    s2 = build_stats(cells, grades, crit, 5.0, warns, {})
    assert s2["coverage_pct"] == round(100 * 2 / 3, 1)  # 기존 셀 기반 유지
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_stats.py -v`
Expected: 신규 1개 FAIL (unexpected keyword)

- [ ] **Step 3: 구현**

`stats.py`: 시그니처를 `def build_stats(cells, grades, crit, u_mm, warnings, meta, zones=None, coverage_pct=None):`로. 반환 dict의 coverage 줄을:
```python
        "coverage_pct": (round(float(coverage_pct), 1) if coverage_pct is not None
                         else (round(100 * len(valid) / n, 1) if n else 0.0)),
```
`pipeline.py`: `build_stats(..., zones=zones_out)` 호출에 `coverage_pct=coverage` 추가하고, 그 아래 `stats["coverage_pct"] = coverage` 덮어쓰기 줄 삭제.

- [ ] **Step 4: 통과 확인 + 전체 스위트**

Run: `python -m pytest tests/test_stats.py tests/test_pipeline.py -v && python -m pytest -q`
Expected: 79 PASS (78+1), 회귀 없음

- [ ] **Step 5: Commit**

```bash
git add engine/flatness/outputs/stats.py engine/flatness/core/pipeline.py engine/tests/test_stats.py
git commit -m "refactor(engine): coverage_pct를 build_stats 파라미터로 편입 (티켓 16)"
```

---

### Task 2: 벽 픽스처 + 컬럼 그리드 + 벽 라인 RANSAC

**Files:**
- Create: `engine/flatness/core/walls.py`
- Modify: `engine/tests/fixtures/synthetic.py` (flat_wall 추가)
- Test: `engine/tests/test_walls.py`

**Interfaces:**
- Produces:
  - 픽스처 `flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0, axis='x', noise_sd=0.0, seed=0) -> np.ndarray (n,3)` — axis='x'면 x∈[0,length]·y=y0(법선 y), axis='y'면 y∈[0,length]·x=y0. z∈[0,height]. 법선 방향으로 noise_sd 가우시안
  - `WallLine` dataclass: `p0(2,), direction(2,), normal(2,)` (단위벡터, bbox 상대 m), `u_min, u_max, z_min, z_max` (float)
  - `build_column_grid(chunks, info, scale_to_m, bin_m=0.05) -> (zmin2d, zmax2d, cnt2d, origin(2,), shape)` — xy 컬럼별 z 최소/최대/점수 누적(스트리밍, bbox 상대 좌표)
  - `detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, bin_m, min_height_m=0.8, min_length_m=1.0, dist_thresh_m=0.05, min_cells=40, max_walls=8, n_iter=800, seed=0) -> list[WallLine]`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_walls.py
import numpy as np
from tests.fixtures.synthetic import flat_floor, flat_wall
from flatness.io.reader import CloudInfo
from flatness.core.walls import build_column_grid, detect_wall_lines

def _cols(pts, bin_m=0.05):
    info = CloudInfo(len(pts), pts.min(axis=0).astype(float), pts.max(axis=0).astype(float))
    return build_column_grid(iter([pts]), info, 1.0, bin_m=bin_m) , info

def test_flat_wall_fixture_geometry():
    w = flat_wall(length=4.0, height=2.4, spacing=0.05)
    assert abs(w[:, 0].max() - 4.0) < 0.06 and abs(w[:, 2].max() - 2.4) < 0.06
    assert np.allclose(w[:, 1], 0.0)
    wy = flat_wall(length=3.0, height=2.4, spacing=0.05, axis='y', y0=1.5)
    assert np.allclose(wy[:, 0], 1.5) and abs(wy[:, 1].max() - 3.0) < 0.06

def test_floor_only_no_walls():
    (grids), info = _cols(flat_floor(size=(4.0, 4.0), spacing=0.02))
    zmin2d, zmax2d, cnt2d, origin, shape = grids
    walls = detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, 0.05)
    assert walls == []

def test_one_wall_detected():
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)])
    (zmin2d, zmax2d, cnt2d, origin, shape), info = _cols(pts)
    walls = detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, 0.05)
    assert len(walls) == 1
    w = walls[0]
    assert abs(abs(w.direction[0]) - 1.0) < 0.05      # x축 방향 벽
    assert (w.u_max - w.u_min) > 3.5                   # 길이 ≈ 4m
    assert (w.z_max - w.z_min) > 2.0                   # 높이 ≈ 2.4m

def test_two_perpendicular_walls():
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0),
                     flat_wall(length=3.0, height=2.4, spacing=0.02, axis='y', y0=0.0)])
    (zmin2d, zmax2d, cnt2d, origin, shape), info = _cols(pts)
    walls = detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, 0.05)
    assert len(walls) == 2
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_walls.py -v`
Expected: FAIL (flat_wall / walls 모듈 없음)

- [ ] **Step 3: 픽스처 구현** (`synthetic.py`에 추가)

```python
def flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0, axis='x', noise_sd=0.0, seed=0):
    """수직 벽 점군. axis='x': x가 벽 방향·y=y0(법선 y), axis='y': y가 벽 방향·x=y0."""
    rng = np.random.default_rng(seed)
    us = np.arange(0.0, length + spacing / 2, spacing)
    vs = np.arange(0.0, height + spacing / 2, spacing)
    gu, gv = np.meshgrid(us, vs)
    off = rng.normal(0.0, noise_sd, gu.shape) if noise_sd > 0 else np.zeros_like(gu)
    if axis == 'x':
        return np.column_stack([gu.ravel(), (y0 + off).ravel(), gv.ravel()])
    return np.column_stack([(y0 + off).ravel(), gu.ravel(), gv.ravel()])
```

- [ ] **Step 4: walls.py 구현 (part 1)**

```python
# engine/flatness/core/walls.py
"""벽면 검출·투영 — xy 컬럼 점유에서 벽 후보를 찾고 2D 라인 RANSAC으로 벽 라인 추출.

z-up 가정(스펙 §5.1.8): 수직도는 z축 기준 상대 지표다.
"""
from dataclasses import dataclass
import numpy as np


@dataclass
class WallLine:
    p0: np.ndarray
    direction: np.ndarray
    normal: np.ndarray
    u_min: float
    u_max: float
    z_min: float
    z_max: float


def build_column_grid(chunks, info, scale_to_m, bin_m=0.05):
    """xy 컬럼별 z 최소/최대/점수 누적 (스트리밍, bbox 상대 좌표)."""
    lo = info.bbox_min * scale_to_m
    hi = info.bbox_max * scale_to_m
    nx = max(1, int(np.ceil((hi[0] - lo[0]) / bin_m)))
    ny = max(1, int(np.ceil((hi[1] - lo[1]) / bin_m)))
    zmin2d = np.full((ny, nx), np.inf, dtype=np.float64)
    zmax2d = np.full((ny, nx), -np.inf, dtype=np.float64)
    cnt2d = np.zeros((ny, nx), dtype=np.int32)
    for c in chunks:
        p = c.astype(np.float64) * scale_to_m
        ix = np.clip(((p[:, 0] - lo[0]) / bin_m).astype(np.int64), 0, nx - 1)
        iy = np.clip(((p[:, 1] - lo[1]) / bin_m).astype(np.int64), 0, ny - 1)
        z = p[:, 2] - lo[2]
        flat = iy * nx + ix
        np.minimum.at(zmin2d.ravel(), flat, z)
        np.maximum.at(zmax2d.ravel(), flat, z)
        np.add.at(cnt2d.ravel(), flat, 1)
    return zmin2d, zmax2d, cnt2d, np.zeros(2), (ny, nx)


def detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, bin_m, min_height_m=0.8,
                      min_length_m=1.0, dist_thresh_m=0.05, min_cells=40,
                      max_walls=8, n_iter=800, seed=0):
    """벽 후보 컬럼 중심점들에 2D 라인 RANSAC 반복 추출 (최대 max_walls개)."""
    rng = np.random.default_rng(seed)
    extent = zmax2d - zmin2d
    mask = np.isfinite(extent) & (extent >= min_height_m) & (cnt2d >= 3)
    ys, xs = np.nonzero(mask)
    pts = np.column_stack([origin[0] + (xs + 0.5) * bin_m,
                           origin[1] + (ys + 0.5) * bin_m])
    zmins = zmin2d[ys, xs]
    zmaxs = zmax2d[ys, xs]
    walls = []
    active = np.ones(len(pts), dtype=bool)
    while len(walls) < max_walls:
        idx = np.nonzero(active)[0]
        if len(idx) < min_cells:
            break
        best = None  # (인라이어 수, 인라이어 인덱스 배열, p0, d, n)
        for _ in range(n_iter):
            i, j = rng.choice(idx, 2, replace=False)
            d = pts[j] - pts[i]
            L = np.hypot(d[0], d[1])
            if L < 1e-9:
                continue
            d = d / L
            n = np.array([-d[1], d[0]])
            dist = np.abs((pts[idx] - pts[i]) @ n)
            inl = idx[dist < dist_thresh_m]
            if best is None or len(inl) > len(best[1]):
                best = (len(inl), inl, pts[i].copy(), d.copy(), n.copy())
        if best is None or len(best[1]) < min_cells:
            break
        _, inl, p0, d, n = best
        u = (pts[inl] - p0) @ d
        if float(u.max() - u.min()) < min_length_m:
            active[inl] = False  # 짧은 파편 라인: 소진시키고 계속
            continue
        walls.append(WallLine(p0=p0, direction=d, normal=n,
                              u_min=float(u.min()), u_max=float(u.max()),
                              z_min=float(zmins[inl].min()), z_max=float(zmaxs[inl].max())))
        active[inl] = False
    return walls
```

- [ ] **Step 5: 통과 확인 + 전체 스위트**

Run: `python -m pytest tests/test_walls.py -v && python -m pytest -q`
Expected: 5 PASS(픽스처 1 + 검출 4... 픽스처 테스트 포함 총 4개 함수) + 회귀 없음

- [ ] **Step 6: Commit**

```bash
git add engine/flatness/core/walls.py engine/tests/fixtures/synthetic.py engine/tests/test_walls.py
git commit -m "feat(engine): 벽 픽스처 + 컬럼 그리드 + 벽 라인 RANSAC 검출"
```

---

### Task 3: 벽 점 투영 + 벽 그리드 (기존 서브셀 재사용)

**Files:**
- Modify: `engine/flatness/core/walls.py`
- Test: `engine/tests/test_walls.py` (테스트 추가)

**Interfaces:**
- Consumes: `build_subcell_grid`, `CloudInfo`, Task 2 `WallLine`
- Produces:
  - `project_wall_points(chunks, info, scale_to_m, wall, band_m=0.1, edge_margin_m=0.1) -> list[np.ndarray]` — 벽 평면 ±band 내, **v ≥ z_min+edge_margin**(바닥 접합부의 바닥 점 띠 제외 — 실물 벽 검측도 접합부를 제외) 점을 (u, v, w) float64 (k,3) 청크 리스트로 (u=벽방향, v=높이, w=법선 부호 편차; **+w = 법선 방향 돌출**)
  - `wall_grid(uvw_chunks, subcell_m=0.05) -> SubcellGrid` — (u,v) 평면 그리드에 w 중앙값 (CloudInfo 합성 후 build_subcell_grid 재사용 — 품질 마스크·쌍봉 그대로 적용됨). 주의: build_subcell_grid는 z(=w)를 bbox 상대화하므로 median_z에 w_min 오프셋이 실림 — 평면 재피팅(Task 4)이 상수항으로 흡수하므로 판정 무영향

- [ ] **Step 1: 실패하는 테스트 작성** (`engine/tests/test_walls.py`에 추가)

```python
from flatness.core.walls import project_wall_points, wall_grid

def _detect_one(pts):
    (zmin2d, zmax2d, cnt2d, origin, shape), info = _cols(pts)
    walls = detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, 0.05)
    assert len(walls) == 1
    return walls[0], info

def test_projection_flat_wall_w_near_zero():
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)])
    wall, info = _detect_one(pts)
    uvw = project_wall_points(iter([pts]), info, 1.0, wall)
    allp = np.vstack(uvw)
    assert abs(float(np.abs(allp[:, 2]).max())) < 0.06  # 밴드 내, 벽면 w ≈ 0(바닥점은 제외됨)
    g = wall_grid(uvw)
    assert np.isfinite(g.median_z).sum() > 1000          # 벽 영역 유효 서브셀 다수

def test_projection_excludes_floor_points():
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)])
    wall, info = _detect_one(pts)
    uvw = np.vstack(project_wall_points(iter([pts]), info, 1.0, wall))
    # 바닥은 벽 평면에서 y로 멀어지는 점 대부분 제외 — 남는 점은 벽 근처 좁은 띠뿐
    n_wall = len(flat_wall(length=4.0, height=2.4, spacing=0.02))
    assert len(uvw) < n_wall * 1.5

def test_wall_bump_visible_in_grid():
    w = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
    r = np.hypot(w[:, 0] - 2.0, w[:, 2] - 1.2)
    m = r < 0.3
    w[m, 1] += 0.01 * 0.5 * (1.0 + np.cos(np.pi * r[m] / 0.3))  # 법선(y) 방향 10mm 돌출
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02), w])
    wall, info = _detect_one(pts)
    g = wall_grid(project_wall_points(iter([pts]), info, 1.0, wall))
    base = float(np.nanmedian(g.median_z))
    assert 0.008 < float(np.nanmax(g.median_z)) - base < 0.012  # 돌출이 w 중앙값에 보존
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_walls.py -v`
Expected: 신규 3개 FAIL

- [ ] **Step 3: 구현** (`walls.py`에 추가)

```python
from flatness.io.reader import CloudInfo
from flatness.core.subcell import build_subcell_grid


def project_wall_points(chunks, info, scale_to_m, wall, band_m=0.1, edge_margin_m=0.1):
    """벽 평면 ±band 내 점을 (u, v, w) float64 청크로 변환. +w = 법선 방향 돌출.

    바닥 접합부(v < z_min+edge_margin)는 제외 — 밴드 안에 들어오는 바닥 점 띠가
    하단 행을 오염시키는 것을 막는다(실물 벽 검측도 접합부 제외).
    """
    lo = info.bbox_min * scale_to_m
    out = []
    for c in chunks:
        p = c.astype(np.float64) * scale_to_m
        xy = p[:, :2] - lo[:2]
        rel = xy - wall.p0
        w = rel @ wall.normal
        u = rel @ wall.direction
        v = p[:, 2] - lo[2]
        m = (np.abs(w) <= band_m) & (u >= wall.u_min) & (u <= wall.u_max) \
            & (v >= wall.z_min + edge_margin_m) & (v <= wall.z_max)
        if m.any():
            out.append(np.column_stack([u[m], v[m], w[m]]))
    return out


def wall_grid(uvw_chunks, subcell_m=0.05):
    """(u,v,w) 점으로 서브셀 그리드 구성 — 기존 비닝·품질 마스크 전부 재사용."""
    allp = np.vstack(uvw_chunks) if uvw_chunks else np.zeros((0, 3))
    if len(allp) == 0:
        raise ValueError("벽 투영 점 없음")
    info = CloudInfo(len(allp), allp.min(axis=0), allp.max(axis=0))
    return build_subcell_grid(iter([allp]), info, 1.0, subcell_m=subcell_m)
```

- [ ] **Step 4: 통과 확인 + 전체 스위트**

Run: `python -m pytest tests/test_walls.py -v && python -m pytest -q`
Expected: 전부 PASS + 회귀 없음

- [ ] **Step 5: Commit**

```bash
git add engine/flatness/core/walls.py engine/tests/test_walls.py
git commit -m "feat(engine): 벽 점 로컬 투영 + 서브셀 그리드 재사용"
```

---

### Task 4: 벽 평가 (잔차 셀 판정 + 수직도)

**Files:**
- Modify: `engine/flatness/core/walls.py`
- Test: `engine/tests/test_walls.py` (테스트 추가)

**Interfaces:**
- Consumes: `fit_plane_ransac`, `residual_grid`, `evaluate_cells`, `grade_cells`, `grade_value`, `load_criteria`
- Produces: `evaluate_wall(grid, criterion, u_mm) -> (cells, grades, warns, wall_metrics)` — wall_metrics dict: `{height_m, length_m, plumbness_mm, plumb_grade, plane_abc}`. 셀 판정은 벽 평면(u,v→w) 잔차의 직선자, 수직도는 평면 계수 b(=dw/dv)×H

- [ ] **Step 1: 실패하는 테스트 작성**

```python
from flatness.core.walls import evaluate_wall
from flatness.criteria import load_criteria

CRIT_WALL = load_criteria()["wall-kcs-tilt-other"]  # flatness 3m/9mm

def test_tilted_wall_plumbness_and_flat_cells():
    # 기울어진 벽(w = 0.005×v): 2.4m 높이 → 수직도 12mm, 셀 잔차는 ≈0(기울기 분리)
    w = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
    w[:, 1] += 0.005 * w[:, 2]
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02), w])
    wall, info = _detect_one(pts)
    g = wall_grid(project_wall_points(iter([pts]), info, 1.0, wall))
    cells, grades, warns, wm = evaluate_wall(g, CRIT_WALL, 8.0)
    assert 10.0 <= wm["plumbness_mm"] <= 14.0
    assert wm["plumb_grade"] == "pass"          # 12 ≤ b1=25−8=17
    valid = [c for c in cells if c.value_mm is not None]
    assert len(valid) >= 4 and all(c.value_mm < 1.0 for c in valid)

def test_wall_bump_graded():
    w = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
    r = np.hypot(w[:, 0] - 2.0, w[:, 2] - 1.2)
    m = r < 0.3
    w[m, 1] -= 0.012 * 0.5 * (1.0 + np.cos(np.pi * r[m] / 0.3))  # 함몰 12mm(법선 반대)
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02), w])
    wall, info = _detect_one(pts)
    g = wall_grid(project_wall_points(iter([pts]), info, 1.0, wall))
    cells, grades, warns, wm = evaluate_wall(g, CRIT_WALL, 8.0)
    worst = max((c for c in cells if c.value_mm is not None), key=lambda c: c.value_mm)
    assert 11.0 <= worst.value_mm <= 13.0       # 함몰=깊이 정확 (±1mm 게이트)
    assert abs(worst.worst_x - 2.0) < 1.0 and abs(worst.worst_y - 1.2) < 1.0
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_walls.py -v`
Expected: 신규 2개 FAIL (evaluate_wall 없음)

- [ ] **Step 3: 구현** (`walls.py`에 추가)

```python
from flatness.core.plane import fit_plane_ransac, residual_grid
from flatness.core.cells import evaluate_cells
from flatness.criteria import grade_cells, grade_value, load_criteria


def evaluate_wall(grid, criterion, u_mm):
    """벽 평면 재피팅 잔차의 셀 직선자 판정 + 수직도(b=dw/dv) 산출.

    셀 판정 = 국부 요철(평면 제거 후), 수직도 = 전역 기울기 — 바닥의 레벨 분리와 동일 원칙.
    """
    ys, xs = np.nonzero(np.isfinite(grid.median_z))
    if len(xs) < 10:
        raise ValueError("벽 유효 서브셀 부족")
    cu = (xs + 0.5) * grid.size_m
    cv = (ys + 0.5) * grid.size_m
    a, b, c = fit_plane_ransac(cu, cv, grid.median_z[ys, xs].astype(float))
    residuals = residual_grid(grid, (a, b, c))
    span = criterion.span_m if criterion.span_m else 3.0
    cells = evaluate_cells(residuals, grid, span_m=span, cell_m=1.0)
    grades, warns = grade_cells(cells, criterion, u_mm)
    height = float(cv.max() - cv.min())
    length = float(cu.max() - cu.min())
    plumb_mm = abs(b) * height * 1000.0  # b = 높이당 법선 편차(z-up 기준 상대 수직도)
    pc = load_criteria()["wall-kcs-plumb"]
    plumb_grade, _ = grade_value(plumb_mm, pc, u_mm, span_used_m=1.0)
    metrics = {"height_m": round(height, 2), "length_m": round(length, 2),
               "plumbness_mm": round(plumb_mm, 2), "plumb_grade": plumb_grade,
               "plane_abc": [round(v, 6) for v in (a, b, c)]}
    return cells, grades, list(warns), metrics
```

- [ ] **Step 4: 통과 확인 + 전체 스위트**

Run: `python -m pytest tests/test_walls.py -v && python -m pytest -q`
Expected: 전부 PASS + 회귀 없음

- [ ] **Step 5: Commit**

```bash
git add engine/flatness/core/walls.py engine/tests/test_walls.py
git commit -m "feat(engine): 벽 평가 (잔차 셀 판정 + 수직도 자동 병행)"
```

---

### Task 5: analyze_wall 파이프라인 + CLI 벽 지원

**Files:**
- Modify: `engine/flatness/core/pipeline.py` (analyze_wall 추가), `engine/flatness/cli.py`
- Test: `engine/tests/test_pipeline.py`, `engine/tests/test_cli.py` (테스트 추가)

**Interfaces:**
- Produces:
  - `analyze_wall(path, scale_to_m, criterion, u_mm, out_dir, subcell_m=0.05, cell_m=1.0, chunk_size=2_000_000) -> dict` — 벽별 히트맵 `heatmap_wall{n}.png`, stats["walls"]=[{wall_id, ...evaluate_wall metrics, n_cells}], 셀은 전 벽 합산(zone_id=wall_id), 경고에 `plumbness_relative_to_z` 고정 포함. 벽 미검출 시 ValueError("벽면 미검출: 스캔 범위 또는 파라미터 확인")
  - CLI: criterion.surface=="wall"이면 analyze_wall 호출(기존 floor 전용 가드 교체). `--criteria`의 metric이 plumbness면 exit 1("수직도는 자동 병행 판정: flatness 기준을 선택"). `--uncertainty-mm` 기본값을 None으로 바꾸고 미지정 시 바닥 5.0/벽 8.0. 성공 출력에 벽 수·벽별 수직도 줄 추가(특수기호 금지)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_pipeline.py 에 추가
def test_wall_end_to_end(tmp_path):
    from flatness.core.pipeline import analyze_wall
    from tests.fixtures.synthetic import flat_wall
    w = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
    r = np.hypot(w[:, 0] - 2.0, w[:, 2] - 1.2)
    m = r < 0.3
    w[m, 1] -= 0.012 * 0.5 * (1.0 + np.cos(np.pi * r[m] / 0.3))
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02), w,
                     flat_wall(length=3.0, height=2.4, spacing=0.02, axis='y', y0=0.0)])
    write_binary_ply(pts, tmp_path / "room.ply")
    crit = load_criteria()["wall-kcs-tilt-other"]
    stats = analyze_wall(tmp_path / "room.ply", 1.0, crit, 8.0, tmp_path / "out")
    assert len(stats["walls"]) == 2
    assert "plumbness_relative_to_z" in stats["warnings"]
    assert 11.0 <= stats["worst"]["value_mm"] <= 13.0
    assert (tmp_path / "out" / "heatmap_wall1.png").exists()
    assert (tmp_path / "out" / "heatmap_wall2.png").exists()
```

```python
# engine/tests/test_cli.py 에 추가
def test_wall_criteria_cli(tmp_path):
    from tests.fixtures.synthetic import flat_wall
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)])
    write_binary_ply(pts, tmp_path / "room.ply")
    r = _run("analyze", str(tmp_path / "room.ply"), "--out", str(tmp_path / "out"),
             "--units", "m", "--criteria", "wall-kcs-tilt-other")
    assert r.returncode == 0, r.stderr
    assert "벽" in r.stdout

def test_plumbness_criteria_rejected(tmp_path):
    write_binary_ply(flat_floor(size=(2.0, 2.0), spacing=0.02), tmp_path / "s.ply")
    r = _run("analyze", str(tmp_path / "s.ply"), "--out", str(tmp_path / "out"),
             "--units", "m", "--criteria", "wall-kcs-plumb")
    assert r.returncode == 1
    assert "자동 병행" in r.stdout
```
(test_cli.py 상단에 `import numpy as np` 없으면 추가)

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_pipeline.py::test_wall_end_to_end tests/test_cli.py -v`
Expected: 신규 FAIL

- [ ] **Step 3: analyze_wall 구현** (`pipeline.py`에 추가)

```python
from dataclasses import replace as _dc_replace
from flatness.core.walls import (build_column_grid, detect_wall_lines,
                                 project_wall_points, wall_grid, evaluate_wall)


def analyze_wall(path, scale_to_m, criterion, u_mm, out_dir,
                 subcell_m=0.05, cell_m=1.0, chunk_size=2_000_000):
    """벽면 분석 오케스트레이션 — 벽 검출 → 벽별 투영·판정 → 통합 stats (스펙 §5.1.8)."""
    info = read_info(path, chunk_size=chunk_size)
    cols = build_column_grid(iter_chunks(path, chunk_size=chunk_size), info, scale_to_m)
    zmin2d, zmax2d, cnt2d, origin, _ = cols
    walls = detect_wall_lines(zmin2d, zmax2d, cnt2d, origin, 0.05)
    if not walls:
        raise ValueError("벽면 미검출: 스캔 범위 또는 파라미터 확인")
    out_dir.mkdir(parents=True, exist_ok=True)
    all_cells, all_grades, walls_out = [], [], []
    warns = {"plumbness_relative_to_z"}  # z-up 기준 상대 수직도 고지(스펙 §5.1.8)
    for i, wall in enumerate(walls, 1):
        uvw = project_wall_points(iter_chunks(path, chunk_size=chunk_size),
                                  info, scale_to_m, wall)
        grid = wall_grid(uvw, subcell_m=subcell_m)
        if int(np.isfinite(grid.median_z).sum()) < 10:
            continue  # 점이 희박한 파편 벽은 건너뜀
        cells, grades, w, wm = evaluate_wall(grid, criterion, u_mm)
        cells = [_dc_replace(c, zone_id=i) for c in cells]
        render_heatmap(cells, grades, out_dir / f"heatmap_wall{i}.png", cell_m=cell_m)
        all_cells.extend(cells)
        all_grades.extend(grades)
        warns.update(w)
        walls_out.append({"wall_id": i, "n_cells": len(cells), **wm})
    if not walls_out:
        raise ValueError("벽면 미검출: 유효 점이 있는 벽 없음")
    meta = {"file": str(path), "n_points": info.n_points, "scale_to_m": scale_to_m,
            "subcell_m": subcell_m, "cell_m": cell_m, "engine_version": "p1c-0.3.0",
            "surface": "wall"}
    stats = build_stats(all_cells, all_grades, criterion, u_mm, sorted(warns), meta)
    stats["walls"] = walls_out
    write_outputs(out_dir, stats, all_cells, all_grades)
    return stats
```
주의: `render_heatmap`은 셀 ix/iy 기준이라 벽별 호출(벽마다 독립 그리드)이 맞다. `write_outputs`는 전 벽 합산 셀로 1회.

- [ ] **Step 4: CLI 수정** (`cli.py`)

- `--uncertainty-mm` 기본값 `default=None`으로 변경
- 기준 로드 후 가드 교체:
```python
    if crit.metric == "plumbness":
        print("오류: 수직도는 자동 병행 판정: flatness 기준을 선택하세요 (flatness list-criteria)")
        return 1
    u_mm = args.uncertainty_mm if args.uncertainty_mm is not None else (8.0 if crit.surface == "wall" else 5.0)
```
- 분석 분기:
```python
    from flatness.core.pipeline import analyze_floor, analyze_wall
    try:
        if crit.surface == "wall":
            stats = analyze_wall(args.file, _SCALES[args.units], crit, u_mm, args.out)
        else:
            stats = analyze_floor(args.file, _SCALES[args.units], crit, u_mm, args.out)
```
- 성공 출력: surface=wall이면 구역 줄 대신:
```python
    for wnfo in stats.get("walls", []):
        print(f"  벽 {wnfo['wall_id']}: 길이 {wnfo['length_m']}m 높이 {wnfo['height_m']}m"
              f"  수직도 {wnfo['plumbness_mm']}mm ({wnfo['plumb_grade']})")
```
(기존 구역 줄은 `stats.get("zones")`가 있을 때만 출력하도록 조건 추가. U 표기는 산출된 u_mm 사용 — 기존 문자열 유지)

- [ ] **Step 5: 통과 확인 + 전체 스위트**

Run: `python -m pytest tests/test_pipeline.py tests/test_cli.py -v && python -m pytest -q`
Expected: 전부 PASS + 회귀 없음

- [ ] **Step 6: Commit**

```bash
git add engine/flatness/core/pipeline.py engine/flatness/cli.py engine/tests/test_pipeline.py engine/tests/test_cli.py
git commit -m "feat(engine): analyze_wall 파이프라인 + CLI 벽면 지원"
```

---

### Task 6: 경사 추종 구역화 + low_coverage 경고 (티켓 13·14)

**Files:**
- Modify: `engine/flatness/core/zones.py`, `engine/flatness/core/pipeline.py`
- Test: `engine/tests/test_zones.py`, `engine/tests/test_pipeline.py` (테스트 추가)

**Interfaces:**
- Produces: `build_zones(..., grow_iters=12)` — 초기 레벨 밴드 컴포넌트 확정 후, ok 구역마다 [평면 피팅 → 4-인접 미할당 서브셀 중 |mz−plane|≤band 흡수]를 반복(전 구역 무변화 시 조기 종료). 잔차·n_subcells·area_m2는 성장 후 기준으로 산출. 파이프라인은 coverage < 70 → warnings `low_coverage`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_zones.py 에 추가
def test_sloped_slab_full_coverage():
    # 티켓 13: 2.5% 경사에서 고정 높이 밴드는 커버리지 45.8%로 붕괴했음(P1b 최종 리뷰 실측)
    g = _grid(flat_floor(size=(6.0, 6.0), spacing=0.02, tilt=(0.025, 0.0)))
    zmap, res = build_zones(g, detect_levels(g.median_z))
    ok = [z for z in zmap.zones if z.status == "ok"]
    assert len(ok) == 1
    n_valid = int(np.isfinite(g.median_z).sum())
    assert (zmap.labels > 0).sum() >= 0.95 * n_valid   # 평면 추종 성장으로 회복
    assert np.nanmax(np.abs(res)) < 5e-4               # 경사 제거 후 잔차 ≈ 0
```

```python
# engine/tests/test_pipeline.py 에 추가
def test_low_coverage_warning(tmp_path):
    # 티켓 14: 바닥 절반이 판정 불가 수준이면 low_coverage 경고
    a = flat_floor(size=(3.0, 3.0), spacing=0.02)
    junk = flat_floor(size=(3.0, 3.0), spacing=0.02, tilt=(0.4, 0.0))  # 40% 급경사(비바닥)
    junk[:, 0] += 3.2
    write_binary_ply(np.vstack([a, junk]), tmp_path / "s.ply")
    stats = analyze_floor(tmp_path / "s.ply", 1.0, CRIT, 5.0, tmp_path / "out")
    assert stats["coverage_pct"] < 70.0
    assert "low_coverage" in stats["warnings"]
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_zones.py::test_sloped_slab_full_coverage tests/test_pipeline.py::test_low_coverage_warning -v`
Expected: FAIL (커버리지 붕괴 / 경고 없음)

- [ ] **Step 3: zones.py 성장 구현**

`build_zones` 시그니처에 `grow_iters=12` 추가. 잔차 산출 루프 **이전**(구역 목록·labels 확정 직후, main 계산 다음)에 삽입:

```python
    # 평면 추종 영역 성장(티켓 13): 고정 높이 밴드가 놓친 경사 슬래브 영역 회복
    from scipy.ndimage import binary_dilation
    finite = np.isfinite(mz)
    for _ in range(grow_iters):
        changed = False
        for z in zones:
            if z.status != "ok":
                continue
            m = labels == z.zone_id
            ys, xs = np.nonzero(m)
            if len(xs) < 3:
                continue
            cx = (xs + 0.5) * grid.size_m
            cy = (ys + 0.5) * grid.size_m
            a, b, c = fit_plane_ransac(cx, cy, mz[ys, xs].astype(float))
            frontier = binary_dilation(m) & (labels == 0) & finite
            fy, fx = np.nonzero(frontier)
            if len(fx) == 0:
                continue
            pv = mz[fy, fx] - (a * (fx + 0.5) * grid.size_m + b * (fy + 0.5) * grid.size_m + c)
            absorb = np.abs(pv) <= band_m
            if absorb.any():
                labels[fy[absorb], fx[absorb]] = z.zone_id
                changed = True
        if not changed:
            break
    for z in zones:  # 성장 반영 재집계
        n_sub = int((labels == z.zone_id).sum())
        z.n_subcells = n_sub
        z.area_m2 = n_sub * sub_area
```
(main 재선정은 하지 않음 — 성장은 면적만 키우므로 기존 main 유지가 안전. 잔차 산출 루프는 기존 그대로 성장된 labels를 사용)

`pipeline.py` analyze_floor: 경고 블록에 추가:
```python
    if coverage < 70.0:
        warns.append("low_coverage")  # 바닥 인식률 저하(티켓 14, 스펙 §5.3)
```
(주의: `coverage` 계산이 warns 사용부보다 뒤라면 계산을 앞으로 이동)

- [ ] **Step 4: 통과 확인 + 전체 스위트 (회귀 중점: two-rooms·유령·가구)**

Run: `python -m pytest tests/test_zones.py tests/test_pipeline.py -v && python -m pytest -q`
Expected: 전부 PASS

- [ ] **Step 5: Commit**

```bash
git add engine/flatness/core/zones.py engine/flatness/core/pipeline.py engine/tests/test_zones.py engine/tests/test_pipeline.py
git commit -m "feat(engine): 평면 추종 구역 성장 + low_coverage 경고 (티켓 13·14)"
```

---

### Task 7: 통합 스모크 + 백로그 표기

**Files:**
- Modify: `docs/superpowers/plans/2026-07-28-p1b-backlog-notes.md`

- [ ] **Step 1: 전체 스위트** — `cd engine && python -m pytest -q` (약 90개 예상, 출력 리포트에)

- [ ] **Step 2: 수동 스모크(임시 산출물, 커밋 금지)** — 바닥+벽2면(한 벽에 12mm 함몰) 합성 PLY 생성 → `flatness analyze ... --criteria wall-kcs-tilt-other` 실행 → exit 0, 벽 2면·수직도 출력, heatmap_wall1.png 확인 후 정리

```bash
cd engine && python -c "
import numpy as np
from tests.fixtures.synthetic import flat_floor, flat_wall, write_binary_ply
w = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
r = np.hypot(w[:,0]-2.0, w[:,2]-1.2); m = r < 0.3
w[m,1] -= 0.012*0.5*(1.0+np.cos(np.pi*r[m]/0.3))
pts = np.vstack([flat_floor(size=(4.0,3.0), spacing=0.02), w,
                 flat_wall(length=3.0, height=2.4, spacing=0.02, axis='y', y0=0.0)])
write_binary_ply(pts, 'demo_wall.ply')
" && python -m flatness.cli analyze demo_wall.ply --units m --out demo_wall_out --criteria wall-kcs-tilt-other && rm -f demo_wall.ply
```

- [ ] **Step 3: 백로그 표기** — 티켓 13·14·16 줄 앞에 `[해결: P1c]` 추가 후 커밋 `docs: P1c 해결 티켓 표기`
