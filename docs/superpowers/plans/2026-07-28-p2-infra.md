# P2 인프라(Supabase Free 스키마 + 로컬 워커) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Supabase Free용 스키마 SQL(테이블·RLS·잡 큐 함수·시드)과 로컬 폴링 워커(엔진 잡 실행·로컬 산출물)를 구축하고, stats 스키마 계약 문서로 티켓 9·22·25·27을 해소한다. 실 DB 검증은 사용자 자격증명 제공 후(셋업 가이드 포함) — 이 계획의 테스트는 전부 자격증명 불필요(FakeDB·엔진 직접 호출).

**Architecture:** 워커는 DB 접근을 `DBClient` 인터페이스 뒤에 두고(FakeDB로 단위 테스트, `SupabaseRest`는 httpx→PostgREST/RPC 얇은 어댑터), 잡 클레임·전이는 SQL 함수(RPC)로 원자화. 파일은 스펙 §6.3 데모 규약대로 로컬 `data/` 디렉터리. 엔진(`flatness`)은 로컬 경로 의존성으로 재사용.

**Tech Stack:** Python 3.11+(httpx, python-dotenv, 기존 flatness-engine), PostgreSQL 15/Supabase(SQL만 — 로컬 실행 환경 없음: Docker·psql 부재 실측), pytest.

**Spec:** §3(데모 구성)·§6(스키마·RLS·잡 큐)·§6.3(로컬 경로 규약) · **백로그:** 티켓 9(=22·27 스키마 계약 문서), 25(§5.4 JSON 스키마)

## Global Constraints

- **자격증명 불필요 테스트만**: Docker·psql 부재(실측) — SQL은 리뷰·사용자 적용으로 검증, 워커는 FakeDB(동일 인터페이스 인메모리)로 검증. 실 Supabase 연결 코드는 어댑터 1개 파일로 격리
- 스키마는 스펙 §6.1~6.3을 정본으로 구현(테이블 11·부분 유니크·복합 FK·RLS·SECURITY DEFINER 함수). 데모 조정: Storage 경로 컬럼은 로컬 `data/` 상대 경로 문자열, photos 버킷은 P3에서
- criteria 시드는 `engine/flatness/data/seed_criteria.json`의 11행과 **수치 완전 일치**(source 포함), app_settings에 U(바닥 5/벽 8)
- 잡 클레임은 `FOR UPDATE SKIP LOCKED` SQL 함수로만, 전이는 jobs+analyses 동일 트랜잭션(fn_ 함수), 재시도 max_attempts=3·백오프 run_after
- 워커는 단일 프로세스 직렬(잡 type 불문 동시 1개 — 스펙 §3.1), 폴링 간격 기본 3s, SIGINT 정상 종료
- 로컬 경로 규약(스펙 §6.3): `data/raw-scans/{site_id}/{scan_id}/raw.{ext}`, `data/artifacts/{analysis_id}/…` — 불변 ID만
- stats 계약 문서는 백로그 27의 조건부 키 목록·coverage 3중 의미·§5.4 JSON 스키마(티켓 25)를 반드시 포함
- 워커 산출 오류 문자열 cp949 안전, 코드 주석 한국어, 커밋 트레일러 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 엔진 스위트(engine/) 114+1 불변. 워커 테스트는 `worker/`에서 `python -m pytest`

---

### Task 1: stats 스키마 계약 문서 (티켓 9·22·25·27)

**Files:**
- Create: `docs/contracts/stats-schema.md`

**Interfaces:** P3 대시보드·P4 보고서·워커가 이 문서를 정본으로 소비. 이후 stats 키 변경은 이 문서 갱신을 동반해야 함(문서에 명시)

- [ ] **Step 1: 문서 작성** — 아래 내용을 전부 포함(항목별 사실은 엔진 소스 `engine/flatness/outputs/stats.py`·`core/pipeline.py`·`importer/colab_csv.py`를 열어 대조·확정하고, 표의 값이 코드와 다르면 코드가 정본):

1. **공통 키(모든 경로)**: n_cells, n_valid, grade_counts{pass,borderline,repair,rework,na}, grade_pct, value_max_mm|None, value_min_mm|None, value_mean_mm|None, value_p95_mm|None, worst{value_mm,cell_ix,cell_iy,point_x,point_y,zone_id}|None, coverage_pct, reduced_span_cells, applied_criteria{name,source,span_m,pass_mm,rework_mm,u_mm}, warnings[list], zones[list], meta{file,n_points,engine_version,surface,bbox_min[3],…}, auto_summary
2. **조건부 키 표**: preview3d_paths(floor만), walls[](wall만: wall_id,n_cells,height_m,length_m,plumbness_mm,plumb_grade,plane_abc,frame{p0,direction,normal,u_min,u_max,z_min,z_max}), zones[](floor: zone_id,level_m,area_m2,status,plane_abc — wall/import는 빈 배열), meta.scale_to_m(임포트 경로 부재), meta.source(임포트만 'colab-import'), meta.subcell_m·cell_m
3. **coverage_pct 3중 의미**: floor=ok 구역 서브셀/유효 서브셀(바닥 인식률), wall·import=유효 셀/전체 셀(셀 유효율) — 소비자는 meta.surface·meta.source로 구분
4. **좌표 프레임**: floor 셀·worst는 절대 스케일 좌표, wall frame·셀(u,v)은 bbox 상대 — meta.bbox_min이 앵커. 역매핑 공식 1줄씩
5. **warnings 코드 사전**: ghost_layer_rescan, ghost_zone_excluded, furniture_excluded, low_coverage, reduced_span, uncertainty_swallows_repair, plumbness_relative_to_z, wall_{i}_skipped(개방 패턴) — 각 의미 1줄
6. **산출물 파일 규약**: stats.json/cells.json(zone_id 포함 행)/results.csv/heatmap.png(floor·import), heatmap_wall{n}.png(wall, 스킵 시 결번), preview3d.png·preview3d_zoom.png(floor)
7. **§5.4 JSON 연계 입력 스키마(티켓 25 해소)**: 외부 프로그램 결과 JSON 임포트 계약 정의 —
```json
{"format": "flatness-import-v1", "surface": "floor",
 "points": [{"x": 0.0, "y": 0.0, "deviation_mm": -3.2}, ...],
 "meta": {"source_program": "...", "measured_at": "YYYY-MM-DD"}}
```
필드 의미·단위(m/mm)·필수 여부 표 + "CSV(colab)와 JSON(범용) 두 임포트 경로" 설명. 구현은 후속(P4/P5 백로그)이며 본 문서가 계약 정본임을 명시
8. 문서 머리에: 정본 선언("stats 키를 바꾸는 변경은 본 문서 갱신 필수"), ENGINE_VERSION 참조

- [ ] **Step 2: 코드 대조 검증** — 문서의 각 키를 grep으로 소스와 대조한 결과(불일치 0)를 커밋 메시지 본문에 한 줄 기록
- [ ] **Step 3: Commit** — `docs: stats 스키마 계약 문서 (티켓 9·22·25·27)`

---

### Task 2: SQL 마이그레이션 001 — 테이블·제약·RLS

**Files:**
- Create: `supabase/migrations/001_schema.sql`

**Interfaces:** 스펙 §6.1~6.3 정본 구현. Task 3(함수·시드)·워커·P3가 이 스키마를 소비

- [ ] **Step 1: SQL 작성** — 다음을 정확히 포함(스펙 §6.1의 컬럼·제약을 그대로, 데모 조정 각주 주석):

```sql
-- 평활도 분석 대시보드 스키마 v1 (스펙 §6, 데모: 파일 경로는 로컬 data/ 상대 규약)
create extension if not exists pgcrypto;

create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text not null,
  is_admin boolean not null default false,
  created_at timestamptz not null default now()
);

create table app_settings (
  key text primary key,
  value jsonb not null,
  updated_at timestamptz not null default now()
);

create table sites (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  address text,
  memo text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table locations (
  id uuid primary key default gen_random_uuid(),
  site_id uuid not null references sites(id) on delete cascade,
  building text not null default '',
  floor text not null default '',
  floor_order int not null default 0,
  room text not null default '',
  name text not null,
  memo text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (site_id, building, floor, room, name)
);

create type surface_type as enum ('floor', 'wall');
create type scan_status as enum ('uploaded', 'awaiting_unit_confirm', 'ready', 'archived', 'failed');
create type data_lineage as enum ('raw', 'fused_mesh', 'unknown');

create table criteria (
  id uuid primary key default gen_random_uuid(),
  site_id uuid references sites(id) on delete cascade,
  surface surface_type not null,
  name text not null,
  source_text text not null,
  thresholds jsonb not null check (jsonb_typeof(thresholds) = 'array'),
  is_default boolean not null default false,
  is_active boolean not null default true,
  version int not null default 1,
  supersedes_id uuid references criteria(id),
  created_at timestamptz not null default now(),
  unique (id, surface)
);
create unique index criteria_global_name on criteria(surface, name) where site_id is null and is_active;
create unique index criteria_site_name on criteria(site_id, surface, name) where site_id is not null and is_active;
create unique index criteria_global_default on criteria(surface) where site_id is null and is_default and is_active;
create unique index criteria_site_default on criteria(site_id, surface) where site_id is not null and is_default and is_active;

create table scans (
  id uuid primary key default gen_random_uuid(),
  location_id uuid not null references locations(id) on delete cascade,
  surface surface_type not null,
  scanned_at date not null,
  device text,
  operator_id uuid references profiles(id),
  operator_name_manual text,
  selected_criteria_id uuid references criteria(id) on delete restrict,
  raw_file_path text,            -- 데모: data/raw-scans/{site_id}/{scan_id}/raw.{ext}
  original_filename text,
  file_format text,
  point_count bigint,
  unit_scale double precision,
  lineage data_lineage not null default 'unknown',
  status scan_status not null default 'uploaded',
  deleted_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (id, surface)
);

create type analysis_status as enum ('queued', 'processing', 'done', 'failed');
create type verdict as enum ('pass', 'borderline', 'repair', 'rework');

create table analyses (
  id uuid primary key default gen_random_uuid(),
  scan_id uuid not null,
  surface surface_type not null,
  criteria_id uuid not null,
  applied_criteria jsonb,
  params jsonb not null default '{}',
  engine_version text,
  status analysis_status not null default 'queued',
  stats jsonb,
  coverage_pct double precision,
  overall_verdict verdict,
  warnings jsonb not null default '[]',
  artifacts_dir text,            -- 데모: data/artifacts/{analysis_id}
  auto_summary text,
  user_summary text,
  is_current boolean not null default false,
  deleted_at timestamptz,
  created_at timestamptz not null default now(),
  created_by uuid references profiles(id),
  foreign key (scan_id, surface) references scans(id, surface) on delete cascade,
  foreign key (criteria_id, surface) references criteria(id, surface) on delete restrict
);
create unique index analyses_current on analyses(scan_id) where is_current and deleted_at is null;

create table photos (
  id uuid primary key default gen_random_uuid(),
  scan_id uuid references scans(id) on delete cascade,
  location_id uuid references locations(id) on delete cascade,
  site_id uuid references sites(id) on delete cascade,
  file_path text not null,
  caption text,
  taken_at date,
  created_at timestamptz not null default now(),
  check (num_nonnulls(scan_id, location_id, site_id) = 1)
);

create type report_status as enum ('draft', 'finalized');

create table reports (
  id uuid primary key default gen_random_uuid(),
  location_id uuid not null references locations(id) on delete restrict,
  title text not null,
  status report_status not null default 'draft',
  snapshot jsonb,
  opinion_text text,
  pdf_path text,
  created_by uuid references profiles(id),
  created_at timestamptz not null default now()
);

create table report_analyses (
  report_id uuid not null references reports(id) on delete cascade,
  analysis_id uuid not null references analyses(id) on delete restrict,
  sort_order int not null default 0,
  primary key (report_id, analysis_id)
);

create type job_type as enum ('precheck', 'analyze', 'import', 'report');
create type job_status as enum ('queued', 'processing', 'done', 'failed');

create table jobs (
  id uuid primary key default gen_random_uuid(),
  type job_type not null,
  payload jsonb not null default '{}',
  status job_status not null default 'queued',
  attempts int not null default 0,
  max_attempts int not null default 3,
  run_after timestamptz not null default now(),
  locked_at timestamptz,
  locked_by text,
  error text,
  created_at timestamptz not null default now(),
  started_at timestamptz,
  finished_at timestamptz
);
create unique index jobs_dedup on jobs(type, (coalesce(payload->>'analysis_id', payload->>'scan_id', payload->>'report_id')))
  where status in ('queued', 'processing');

-- RLS: 내부용 — authenticated 전체 허용, jobs는 service_role 전용(클라이언트 정책 없음),
-- criteria 전역 행·app_settings 수정은 admin만 (스펙 §6.3)
alter table profiles enable row level security;
alter table app_settings enable row level security;
alter table sites enable row level security;
alter table locations enable row level security;
alter table criteria enable row level security;
alter table scans enable row level security;
alter table analyses enable row level security;
alter table photos enable row level security;
alter table reports enable row level security;
alter table report_analyses enable row level security;
alter table jobs enable row level security;

create policy all_auth on sites for all to authenticated using (true) with check (true);
create policy all_auth on locations for all to authenticated using (true) with check (true);
create policy all_auth on scans for all to authenticated using (true) with check (true);
create policy all_auth on analyses for all to authenticated using (true) with check (true);
create policy all_auth on photos for all to authenticated using (true) with check (true);
create policy all_auth on reports for all to authenticated using (true) with check (true);
create policy all_auth on report_analyses for all to authenticated using (true) with check (true);
create policy read_auth on profiles for select to authenticated using (true);
create policy self_upsert on profiles for insert to authenticated with check (id = auth.uid());
create policy self_update on profiles for update to authenticated using (id = auth.uid());
create policy read_auth on app_settings for select to authenticated using (true);
create policy admin_write on app_settings for all to authenticated
  using (exists (select 1 from profiles p where p.id = auth.uid() and p.is_admin))
  with check (exists (select 1 from profiles p where p.id = auth.uid() and p.is_admin));
create policy read_auth on criteria for select to authenticated using (true);
create policy site_write on criteria for all to authenticated
  using (site_id is not null or exists (select 1 from profiles p where p.id = auth.uid() and p.is_admin))
  with check (site_id is not null or exists (select 1 from profiles p where p.id = auth.uid() and p.is_admin));
-- jobs: 클라이언트 정책 없음(service_role은 RLS 우회) — enqueue는 함수로만(002)
```

주의: 스펙 §6.1과 대조하며 작성하되, 컬럼명·제약이 위와 다르게 필요하면 스펙이 정본. Task 3의 함수·시드가 이 스키마를 참조하므로 이름을 바꾸지 말 것.

- [ ] **Step 2: 정합 검토** — 스펙 §6.1의 각 테이블 항목과 SQL을 줄 단위 대조, 차이(데모 조정)는 SQL 주석으로 명시. 검토 결과를 커밋 본문에 기록
- [ ] **Step 3: Commit** — `feat(db): 스키마 마이그레이션 001 (테이블·제약·RLS)`

---

### Task 3: SQL 마이그레이션 002 — 함수·시드

**Files:**
- Create: `supabase/migrations/002_functions_seed.sql`

**Interfaces:** 워커가 RPC로 호출: `fn_job_claim(p_worker text) -> jobs`, `fn_job_complete(p_job_id uuid)`, `fn_job_fail(p_job_id uuid, p_error text)`, `fn_enqueue_job(p_type job_type, p_payload jsonb) -> uuid`, `fn_resolve_criteria(p_site_id uuid, p_surface surface_type) -> setof criteria`, `fn_reap_stuck_jobs(p_timeout_minutes int default 30)`

- [ ] **Step 1: SQL 작성**

```sql
-- 잡 큐 함수 (스펙 §6.1): 클레임은 SKIP LOCKED 원자, 전이는 analyses와 동일 트랜잭션
create or replace function fn_job_claim(p_worker text)
returns jobs language plpgsql security definer as $$
declare v_job jobs;
begin
  select * into v_job from jobs
   where status = 'queued' and run_after <= now()
   order by created_at
   for update skip locked
   limit 1;
  if v_job.id is null then return null; end if;
  update jobs set status = 'processing', locked_at = now(), locked_by = p_worker,
                  attempts = attempts + 1, started_at = coalesce(started_at, now())
   where id = v_job.id;
  if v_job.type = 'analyze' and (v_job.payload ? 'analysis_id') then
    update analyses set status = 'processing' where id = (v_job.payload->>'analysis_id')::uuid;
  end if;
  select * into v_job from jobs where id = v_job.id;
  return v_job;
end $$;

create or replace function fn_job_complete(p_job_id uuid)
returns void language plpgsql security definer as $$
declare v_job jobs;
begin
  select * into v_job from jobs where id = p_job_id;
  update jobs set status = 'done', finished_at = now(), locked_at = null, locked_by = null
   where id = p_job_id;
  -- analyses.status는 워커가 결과 저장 시 함께 갱신(update)하므로 여기선 잡만 종결
end $$;

create or replace function fn_job_fail(p_job_id uuid, p_error text)
returns void language plpgsql security definer as $$
declare v_job jobs;
begin
  select * into v_job from jobs where id = p_job_id;
  if v_job.attempts >= v_job.max_attempts then
    update jobs set status = 'failed', error = p_error, finished_at = now(),
                    locked_at = null, locked_by = null where id = p_job_id;
    if v_job.type = 'analyze' and (v_job.payload ? 'analysis_id') then
      update analyses set status = 'failed' where id = (v_job.payload->>'analysis_id')::uuid;
    end if;
  else
    update jobs set status = 'queued', error = p_error,
                    run_after = now() + (interval '10 seconds' * v_job.attempts),
                    locked_at = null, locked_by = null where id = p_job_id;
    if v_job.type = 'analyze' and (v_job.payload ? 'analysis_id') then
      update analyses set status = 'queued' where id = (v_job.payload->>'analysis_id')::uuid;
    end if;
  end if;
end $$;

create or replace function fn_enqueue_job(p_type job_type, p_payload jsonb)
returns uuid language plpgsql security definer as $$
declare v_id uuid;
begin
  insert into jobs(type, payload) values (p_type, p_payload) returning id into v_id;
  return v_id;
end $$;
grant execute on function fn_enqueue_job(job_type, jsonb) to authenticated;

create or replace function fn_resolve_criteria(p_site_id uuid, p_surface surface_type)
returns setof criteria language sql stable as $$
  select * from criteria
   where is_active and surface = p_surface
     and (site_id = p_site_id or (site_id is null
          and not exists (select 1 from criteria c2 where c2.is_active
                           and c2.surface = p_surface and c2.site_id = p_site_id)))
   order by is_default desc, name;
$$;

create or replace function fn_reap_stuck_jobs(p_timeout_minutes int default 30)
returns int language plpgsql security definer as $$
declare v_count int;
begin
  with reaped as (
    update jobs set status = 'queued', locked_at = null, locked_by = null,
                    run_after = now()
     where status = 'processing' and locked_at < now() - make_interval(mins => p_timeout_minutes)
     returning id, type, payload)
  select count(*) into v_count from reaped;
  update analyses a set status = 'queued'
   from jobs j
   where j.status = 'queued' and j.type = 'analyze'
     and (j.payload->>'analysis_id')::uuid = a.id and a.status = 'processing';
  return v_count;
end $$;

-- app_settings 시드: 측정 불확도 U (스펙 §4.2)
insert into app_settings(key, value) values
  ('uncertainty_mm', '{"floor": 5.0, "wall": 8.0}')
on conflict (key) do nothing;

-- criteria 시드 11종: engine/flatness/data/seed_criteria.json과 수치 완전 일치해야 함
insert into criteria (surface, name, source_text, thresholds, is_default) values
  ('floor', 'floor-kcs-finish7plus', 'KCS 14 20 10 표 3.7-1 (마감두께 7mm 이상)', '[{"span_m":1,"metric":"flatness","pass_mm":10,"rework_mm":30}]', false),
  ('floor', 'floor-kcs-finish7minus', 'KCS 14 20 10 표 3.7-1 (마감두께 7mm 미만)', '[{"span_m":3,"metric":"flatness","pass_mm":10,"rework_mm":30}]', false),
  ('floor', 'floor-kcs-exposed', 'KCS 14 20 10 표 3.7-1 (제물치장·얇은 마감)', '[{"span_m":3,"metric":"flatness","pass_mm":7,"rework_mm":21}]', true),
  ('floor', 'floor-molit-cushion', '국토부 바닥충격음 고시 (완충재 바탕)', '[{"span_m":3,"metric":"flatness","pass_mm":7,"rework_mm":21}]', false),
  ('floor', 'floor-lh-exposed', 'LHCS 14 20 10 05 표 3.11-7 (제물치장·도장·벽지)', '[{"span_m":3,"metric":"flatness","pass_mm":6,"rework_mm":18}]', false),
  ('floor', 'floor-lh-thick', 'LHCS 14 20 10 05 표 3.11-7 (마감 13mm 초과)', '[{"span_m":3,"metric":"flatness","pass_mm":10,"rework_mm":30}]', false),
  ('wall', 'wall-kcs-tilt-exposed', 'KCS 21 50 05 §3.2.6 (노출 모서리·줄눈)', '[{"span_m":3,"metric":"flatness","pass_mm":6,"rework_mm":18}]', false),
  ('wall', 'wall-kcs-tilt-other', 'KCS 21 50 05 §3.2.6 (기타)', '[{"span_m":3,"metric":"flatness","pass_mm":9,"rework_mm":27}]', true),
  ('wall', 'wall-kcs-plumb', 'KCS 21 50 05 §3.2.2 (H≤30m)', '[{"span_m":null,"metric":"plumbness","pass_mm":25,"rework_mm":75}]', false),
  ('wall', 'wall-plaster-surface', '주택건설 전문시방서 31310 (미장 바름면)', '[{"span_m":3,"metric":"flatness","pass_mm":3,"rework_mm":9}]', false),
  ('wall', 'wall-plaster-base', '주택건설 전문시방서 31310 (미장 바탕면)', '[{"span_m":3,"metric":"flatness","pass_mm":6,"rework_mm":18}]', false);
```

- [ ] **Step 2: 시드 대조** — `engine/flatness/data/seed_criteria.json`의 11행과 name/surface/span_m/pass_mm/rework_mm/source를 항목별 대조(불일치 0), 결과를 커밋 본문에 기록
- [ ] **Step 3: Commit** — `feat(db): 잡 큐 함수·기준 시드 마이그레이션 002`

---

### Task 4: 워커 스캐폴드 + config + DBClient 인터페이스/FakeDB

**Files:**
- Create: `worker/pyproject.toml`, `worker/flatworker/__init__.py`, `worker/flatworker/config.py`, `worker/flatworker/db.py`, `worker/tests/__init__.py`, `worker/tests/fake_db.py`, `worker/tests/test_config.py`, `worker/tests/test_fake_db.py`

**Interfaces:**
- `Config` dataclass: `supabase_url: str, service_role_key: str, data_dir: Path, poll_interval_s: float(기본 3.0), worker_id: str` — `load_config(env_path=None)`이 .env→환경변수 순으로 로드, 필수값 부재 시 `ConfigError`(어떤 키가 없는지 명시)
- `DBClient`(추상): `claim_job() -> dict|None`, `complete_job(job_id)`, `fail_job(job_id, error)`, `enqueue_job(type, payload) -> str`, `get_scan(scan_id) -> dict`, `get_criteria(criteria_id) -> dict`, `get_app_setting(key) -> dict`, `update_scan(scan_id, fields)`, `insert_analysis(fields) -> str`, `update_analysis(analysis_id, fields)`, `set_current_analysis(scan_id, analysis_id)`
- `SupabaseRest(DBClient)`: httpx로 PostgREST(`/rest/v1/...`, service_role 헤더)·RPC(`/rest/v1/rpc/fn_job_claim` 등) 호출 — **네트워크 코드는 이 파일 하나에 격리**, 단위 테스트는 httpx 호출을 하지 않음(실검증은 사용자 셋업 후 스모크)
- `FakeDB(DBClient)`(tests/): 인메모리 dict 구현 + `fn_job_fail`의 재시도·백오프 시맨틱 모사 — 이후 태스크 전부 이걸로 테스트

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# worker/tests/test_config.py
import pytest
from flatworker.config import load_config, ConfigError

def test_load_from_env_file(tmp_path):
    (tmp_path / ".env").write_text(
        "SUPABASE_URL=https://x.supabase.co\nSUPABASE_SERVICE_ROLE_KEY=k\n"
        f"DATA_DIR={tmp_path / 'data'}\n", encoding="utf-8")
    cfg = load_config(tmp_path / ".env")
    assert cfg.supabase_url == "https://x.supabase.co"
    assert cfg.poll_interval_s == 3.0
    assert cfg.data_dir.name == "data"

def test_missing_key_raises(tmp_path):
    (tmp_path / ".env").write_text("SUPABASE_URL=https://x.supabase.co\n", encoding="utf-8")
    with pytest.raises(ConfigError, match="SUPABASE_SERVICE_ROLE_KEY"):
        load_config(tmp_path / ".env")
```

```python
# worker/tests/test_fake_db.py
from tests.fake_db import FakeDB

def test_claim_and_complete_lifecycle():
    db = FakeDB()
    jid = db.enqueue_job("analyze", {"analysis_id": "a1"})
    job = db.claim_job()
    assert job["id"] == jid and job["status"] == "processing" and job["attempts"] == 1
    assert db.claim_job() is None  # 동시 클레임 차단(단일 워커 시맨틱)
    db.complete_job(jid)
    assert db.jobs[jid]["status"] == "done"

def test_fail_retries_then_dead():
    db = FakeDB()
    jid = db.enqueue_job("analyze", {"analysis_id": "a1"})
    for i in range(3):
        job = db.claim_job(ignore_backoff=True)
        assert job is not None, f"{i+1}번째 클레임 실패"
        db.fail_job(jid, f"err{i}")
    assert db.jobs[jid]["status"] == "failed"  # max_attempts=3 소진
    assert db.claim_job(ignore_backoff=True) is None
```

- [ ] **Step 2: 실패 확인** — `cd worker && pip install -e . && python -m pytest -q` → FAIL

- [ ] **Step 3: 구현** — pyproject(deps: `httpx>=0.27`, `python-dotenv>=1.0`; dev: pytest; `flatness-engine` 의존은 Task 5에서 로컬 경로로), config.py(dotenv 로드·검증), db.py(DBClient 추상 + SupabaseRest: 각 메서드가 PostgREST/RPC 엔드포인트 1개 호출, 타임아웃 30s, 4xx/5xx는 `DBError(status, body)`), tests/fake_db.py(FakeDB — jobs/scans/analyses/criteria/app_settings dict, claim은 queued&run_after 통과 최고 오래된 것, `claim_job(ignore_backoff=False)` 테스트 편의 인자)

- [ ] **Step 4: 통과 확인** — `python -m pytest -q` (worker/) → 4 PASS. 엔진 스위트 불변 확인(`cd ../engine && python -m pytest -q`)
- [ ] **Step 5: Commit** — `feat(worker): 스캐폴드 + config + DBClient/FakeDB`

---

### Task 5: 로컬 산출물 규약 + precheck/analyze 핸들러

**Files:**
- Create: `worker/flatworker/artifacts.py`, `worker/flatworker/jobs.py`, `worker/tests/test_jobs.py`
- Modify: `worker/pyproject.toml` (deps에 로컬 엔진: `"flatness-engine @ file:../engine"` 대신 **설치 안내 주석** — pip 로컬 경로는 `pip install -e ../engine`으로 문서화, pyproject에는 `"numpy>=1.26"`만 명시하고 flatness 임포트는 설치 전제)

**Interfaces:**
- `artifacts.py`: `raw_scan_dir(data_dir, site_id, scan_id) -> Path`, `artifacts_dir(data_dir, analysis_id) -> Path` — 스펙 §6.3 규약, mkdir 포함
- `jobs.py`:
  - `handle_precheck(db, cfg, payload{scan_id})`: 스캔 raw 파일 read_info→detect_units → `update_scan(scan_id, {"status": "awaiting_unit_confirm", "unit_candidates": ...})` — 데모: 후보를 scans에 저장할 컬럼이 없으므로 **jobs 결과로 두지 않고 scans.unit_scale이 이미 확정(P3 UI가 설정)된 경우 'ready'로 승격**하는 단순 검사로 축소. 미확정이면 awaiting_unit_confirm
  - `handle_analyze(db, cfg, payload{analysis_id})`: analysis→scan→criteria 로드, U는 app_settings, criteria dict를 엔진 `Criterion`으로 변환, `analyze_floor`/`analyze_wall`을 raw 파일·`artifacts_dir`로 실행 → `update_analysis(...: status done, stats, coverage_pct, overall_verdict(=stats 최악 등급), warnings, artifacts_dir, engine_version, applied_criteria, auto_summary)` + `set_current_analysis` — 실패는 예외 전파(runner가 fail_job)
  - `handle_import(db, cfg, payload{analysis_id})`: 동일 흐름으로 `import_colab_csv`
  - `overall_verdict(stats) -> str|None`: rework>0→'rework'>repair>borderline>pass, n_valid=0→None
- 테스트는 FakeDB + 실제 엔진 호출(소형 합성 PLY/CSV를 tmp data_dir에 배치)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# worker/tests/synthetic_helpers.py — 엔진 픽스처를 파일 경로로 로드(패키지명 'tests' 충돌 회피)
import importlib.util
from pathlib import Path

_p = Path(__file__).resolve().parents[2] / "engine" / "tests" / "fixtures" / "synthetic.py"
_spec = importlib.util.spec_from_file_location("engine_synthetic", _p)
synthetic = importlib.util.module_from_spec(_spec)
_spec.loader.exec_module(synthetic)
```

```python
# worker/tests/test_jobs.py
from tests.synthetic_helpers import synthetic
flat_floor, add_bump, write_binary_ply = synthetic.flat_floor, synthetic.add_bump, synthetic.write_binary_ply
from flatworker.config import Config
from flatworker.jobs import handle_analyze, overall_verdict
from flatworker.artifacts import raw_scan_dir, artifacts_dir
from tests.fake_db import FakeDB


def _cfg(tmp_path):
    return Config(supabase_url="http://fake", service_role_key="k",
                  data_dir=tmp_path / "data", poll_interval_s=0.01, worker_id="w1")

def _seed_floor_scan(db, cfg):
    # 함몰 10mm 바닥 스캔 + 분석 행 시드
    pts = add_bump(flat_floor(size=(6.0, 6.0), spacing=0.02), (2.0, 2.0), 0.3, -0.010)
    sd = raw_scan_dir(cfg.data_dir, "site1", "scan1")
    write_binary_ply(pts, sd / "raw.ply")
    db.scans["scan1"] = {"id": "scan1", "site_id": "site1", "surface": "floor",
                         "raw_file_path": str(sd / "raw.ply"), "unit_scale": 1.0,
                         "status": "ready", "selected_criteria_id": "c1"}
    db.criteria["c1"] = {"id": "c1", "surface": "floor", "name": "floor-kcs-exposed",
                         "source_text": "KCS 14 20 10 표 3.7-1 (제물치장·얇은 마감)",
                         "thresholds": [{"span_m": 3, "metric": "flatness", "pass_mm": 7, "rework_mm": 21}]}
    db.app_settings["uncertainty_mm"] = {"floor": 5.0, "wall": 8.0}
    db.analyses["a1"] = {"id": "a1", "scan_id": "scan1", "surface": "floor",
                         "criteria_id": "c1", "status": "queued"}
    return "a1"

def test_handle_analyze_floor(tmp_path):
    db, cfg = FakeDB(), _cfg(tmp_path)
    aid = _seed_floor_scan(db, cfg)
    handle_analyze(db, cfg, {"analysis_id": aid})
    a = db.analyses[aid]
    assert a["status"] == "done"
    assert 9.0 <= a["stats"]["worst"]["value_mm"] <= 11.0
    assert a["overall_verdict"] in ("borderline", "repair")
    assert (artifacts_dir(cfg.data_dir, aid) / "heatmap.png").exists()
    assert a["auto_summary"] and "대체하지 않습니다" in a["auto_summary"]
    assert db.current_analysis.get("scan1") == aid

def test_overall_verdict_priority():
    assert overall_verdict({"n_valid": 5, "grade_counts": {"pass": 1, "borderline": 0, "repair": 2, "rework": 1, "na": 1}}) == "rework"
    assert overall_verdict({"n_valid": 5, "grade_counts": {"pass": 5, "borderline": 0, "repair": 0, "rework": 0, "na": 0}}) == "pass"
    assert overall_verdict({"n_valid": 0, "grade_counts": {"pass": 0, "borderline": 0, "repair": 0, "rework": 0, "na": 3}}) is None
```

- [ ] **Step 2: 실패 확인** → FAIL
- [ ] **Step 3: 구현** — artifacts.py(2함수+mkdir), jobs.py(핸들러 3종+overall_verdict+`_to_criterion(dict)->Criterion` 변환: thresholds[0]에서 span/pass/rework 추출). FakeDB에 `scans/criteria/app_settings/analyses` dict·`update_scan/update_analysis/insert_analysis/set_current_analysis/get_*` 구현 추가(테스트 파일이 아니라 fake_db.py 갱신)
- [ ] **Step 4: 통과 확인** — worker 스위트 PASS + 엔진 114+1 불변
- [ ] **Step 5: Commit** — `feat(worker): 산출물 규약 + precheck/analyze/import 핸들러`

---

### Task 6: 폴링 러너 + 재시도·종료 처리

**Files:**
- Create: `worker/flatworker/runner.py`, `worker/flatworker/__main__.py`, `worker/tests/test_runner.py`

**Interfaces:**
- `run_loop(db, cfg, handlers=None, max_iterations=None)`: claim→dispatch(type별 핸들러)→complete, 예외 시 `fail_job(job_id, str(e)[:500])`, 잡 없으면 poll_interval 슬립. `max_iterations`(테스트용)·SIGINT 핸들링(플래그 방식, 처리 중 잡은 완료 후 종료). 알 수 없는 type은 fail(재시도 무의미하므로 error에 명시)
- `__main__.py`: `python -m flatworker` — load_config→SupabaseRest→run_loop, 시작 로그(cp949 안전)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# worker/tests/test_runner.py
from flatworker.runner import run_loop
from tests.fake_db import FakeDB

def _cfg(tmp_path):
    from flatworker.config import Config
    return Config(supabase_url="http://fake", service_role_key="k",
                  data_dir=tmp_path / "data", poll_interval_s=0.01, worker_id="w1")

def test_runner_dispatch_and_complete(tmp_path):
    db = FakeDB()
    calls = []
    jid = db.enqueue_job("analyze", {"analysis_id": "a1"})
    run_loop(db, _cfg(tmp_path), handlers={"analyze": lambda d, c, p: calls.append(p)},
             max_iterations=3)
    assert calls == [{"analysis_id": "a1"}]
    assert db.jobs[jid]["status"] == "done"

def test_runner_handler_exception_fails_job(tmp_path):
    db = FakeDB()
    jid = db.enqueue_job("analyze", {"analysis_id": "a1"})
    def boom(d, c, p):
        raise ValueError("분석 실패 사유")
    run_loop(db, _cfg(tmp_path), handlers={"analyze": boom}, max_iterations=1)
    assert db.jobs[jid]["status"] == "queued"   # 1회 실패 → 재시도 대기
    assert "분석 실패 사유" in db.jobs[jid]["error"]

def test_runner_unknown_type_fails_terminally(tmp_path):
    db = FakeDB()
    jid = db.enqueue_job("report", {"report_id": "r1"})  # P4 전이라 핸들러 없음
    run_loop(db, _cfg(tmp_path), handlers={}, max_iterations=1)
    assert "핸들러 없음" in db.jobs[jid]["error"]
```

- [ ] **Step 2: 실패 확인** → FAIL
- [ ] **Step 3: 구현** — runner.py(기본 handlers = jobs.py 3종 매핑, 알 수 없는 type은 `fail_job` 후 max_attempts 즉시 소진 처리 대신 error 기록 재시도 — 단순화: fail_job 호출(재시도돼도 무해, error 명시)), __main__.py
- [ ] **Step 4: 통과 확인 + 엔진 불변** — worker 전체 PASS
- [ ] **Step 5: Commit** — `feat(worker): 폴링 러너 + 재시도·종료 처리`

---

### Task 7: 셋업 가이드 + .env.example + README

**Files:**
- Create: `docs/SUPABASE_SETUP.md`, `worker/.env.example`, `worker/README.md`

- [ ] **Step 1: 작성** — SUPABASE_SETUP.md는 다음 단계를 명령·화면 위치까지 구체적으로:
  1. supabase.com 가입/로그인 → New project(Free, 리전 Northeast Asia 권장) — **사용자가 직접 수행**(계정·비밀번호는 시스템이 다루지 않음)
  2. SQL Editor → `supabase/migrations/001_schema.sql` 붙여넣기 실행 → `002_functions_seed.sql` 실행
  3. 검증 쿼리 3개: `select count(*) from criteria;`(=11), `select fn_enqueue_job('analyze','{}'::jsonb);` 후 `select * from fn_job_claim('test');`, `select * from fn_resolve_criteria(null,'floor');`
  4. Settings→API에서 Project URL·service_role key 복사(**service_role은 서버 전용 — 대시보드/브라우저에 노출 금지** 경고), anon key는 P3용으로 별도 기재
  5. `worker/.env` 작성(.env.example 복사) → `pip install -e ../engine && pip install -e .` → `python -m flatworker` 실행 → 시작 로그 확인
  .env.example: SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY/DATA_DIR(기본 ../data)/POLL_INTERVAL_S(기본 3)/WORKER_ID(기본 local-1) + 각 항목 한국어 주석
  worker/README.md: 실행·테스트·구조 1페이지
- [ ] **Step 2: Commit** — `docs: Supabase 셋업 가이드 + 워커 .env.example/README`

---

### Task 8: FakeDB 엔드투엔드 스모크 + 마무리

- [ ] **Step 1:** worker 전체 스위트 + 엔진 스위트(114+1) 확인 (출력 리포트에)
- [ ] **Step 2:** FakeDB E2E 스모크(임시 스크립트, 커밋 금지): 바닥 스캔 시드→`fn`급 흐름(enqueue→run_loop(max_iterations)→analyses done·artifacts 파일 존재) 실행 출력 확인 — test_jobs와 겹치면 러너 경유 통합 테스트 1개를 `worker/tests/test_e2e_fake.py`로 **커밋에 포함**(엔큐→러너→완료→산출물)
- [ ] **Step 3:** 백로그 노트에 `[해결: P2] 티켓 9·22·25·27(스키마 계약 문서)` 표기 + 커밋 `docs: P2 해결 티켓 표기`
