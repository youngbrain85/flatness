# P1d 임포트·3D 프리뷰·종합의견·성능 검증 + 벽 하드닝 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 과업지시서 요구인 기존 결과 임포트(§5.4)·자동 종합의견(§5.3)·3D 프리뷰(§5.1.9)를 추가하고, 30M 점 성능 게이트(§5.2)를 실측하며, P1c 최종 리뷰의 P2 전 필수 티켓 3건(벽별 오류 격리·천장 마진·월드 프레임)을 해소한다.

**Architecture:** 모두 기존 엔진 위의 증축 — 임포트는 이미 평면 제거된 편차를 서브셀→셀 판정 기계에 태우고, 프리뷰는 matplotlib 3D 등각 2장, 종합의견은 stats 규칙 템플릿, 성능은 perf 마커 테스트(기본 스위트 제외).

**Tech Stack:** 기존 engine 패키지 + psutil(dev, 메모리 실측). 신규 모듈 `flatness/importer/colab_csv.py`, `flatness/outputs/summary.py`, `flatness/outputs/preview3d.py`.

**Spec:** §5.3(종합의견)·§5.4(임포트)·§5.1.9(산출물)·§5.2(성능) · **백로그:** 티켓 15·17·18·19·21

## Global Constraints

- 기존 96개 테스트 무수정 통과(승인된 조정 제외), perf 테스트는 `@pytest.mark.perf`로 기본 스위트에서 제외(`pyproject` addopts `-m "not perf"`)
- 사용자 출력·요약 문자열 cp949 안전(U+2014 등 금지), 코드 주석 한국어, 커밋 트레일러 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, 테스트는 engine/에서 `python -m pytest`
- 임포트 결과 meta: `engine_version='external-colab-v1'`, `surface='floor'`, 대시보드·보고서에서 '외부 결과'로 구분 가능해야 함(스펙 §5.4)
- 종합의견은 규칙 템플릿(LLM 금지, 재현성 — 스펙 §5.3), 스크리닝 한계 문구 필수 포함
- ENGINE_VERSION 상수는 `flatness/__init__.py` 단일 정의("p1d-0.4.0") — 두 파이프라인·임포터가 공유, floor meta에도 `surface` 포함(티켓 21)
- 벽 하드닝: 벽별 실패는 스킵+경고(`wall_{i}_skipped`), 천장 마진 `v <= z_max − edge_margin_m`, walls_out에 월드 프레임(p0/direction/normal/u_min/u_max/z_min/z_max) 직렬화(티켓 17·18·19)
- 텍스트 리더: 비유한값(nan/inf) 행 스킵(티켓 15)

---

### Task 1: 벽 하드닝 (티켓 17·18·19)

**Files:**
- Modify: `engine/flatness/core/walls.py` (천장 마진), `engine/flatness/core/pipeline.py` (오류 격리·프레임 직렬화)
- Test: `engine/tests/test_walls.py`, `engine/tests/test_pipeline.py` (추가)

**Interfaces:**
- Produces: `project_wall_points`의 v 상한이 `wall.z_max - edge_margin_m`로 변경. `analyze_wall`은 벽별 ValueError를 스킵+경고(`wall_{i}_skipped`)로 격리, `stats["walls"][j]`에 `frame` 키 추가: `{"p0": [x,y], "direction": [dx,dy], "normal": [nx,ny], "u_min", "u_max", "z_min", "z_max"}` (전부 round 4, bbox 상대 m)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_walls.py 에 추가
def test_ceiling_band_excluded(tmp_path):
    # 티켓 18: 천장 슬래브가 밴드 안으로 들어와도 벽 상단 행을 오염시키지 않는다
    wall = flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)
    ceiling = flat_floor(size=(4.0, 3.0), spacing=0.02)
    ceiling[:, 2] += 2.4  # z=2.4 천장
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02), wall, ceiling])
    w, info = _detect_one(pts)
    uvw = np.vstack(project_wall_points(iter([pts]), info, 1.0, w))
    assert float(uvw[:, 1].max()) <= (w.z_max - 0.1) + 1e-9   # 상단 마진
    assert float(np.abs(uvw[:, 2]).max()) < 0.06               # 천장 점 w 오염 없음
```

```python
# engine/tests/test_pipeline.py 에 추가
def test_wall_error_isolated(tmp_path, monkeypatch):
    # 티켓 17: 한 벽의 평가 실패가 전체 분석을 죽이지 않는다
    from flatness.core import pipeline as pl
    from tests.fixtures.synthetic import flat_wall
    calls = {"n": 0}
    real = pl.evaluate_wall
    def flaky(grid, criterion, u_mm, cell_m=1.0):
        calls["n"] += 1
        if calls["n"] == 2:
            raise ValueError("주입된 벽 평가 실패")
        return real(grid, criterion, u_mm, cell_m=cell_m)
    monkeypatch.setattr(pl, "evaluate_wall", flaky)
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0),
                     flat_wall(length=3.0, height=2.4, spacing=0.02, axis='y', y0=0.0)])
    write_binary_ply(pts, tmp_path / "room.ply")
    crit = load_criteria()["wall-kcs-tilt-other"]
    stats = pl.analyze_wall(tmp_path / "room.ply", 1.0, crit, 8.0, tmp_path / "out")
    assert len(stats["walls"]) == 1                      # 성한 벽만
    assert any(w.startswith("wall_") and w.endswith("_skipped") for w in stats["warnings"])

def test_wall_frame_serialized(tmp_path):
    # 티켓 19: stats만으로 벽 로컬 (u,v)를 월드로 역매핑 가능해야 함
    from tests.fixtures.synthetic import flat_wall
    pts = np.vstack([flat_floor(size=(4.0, 3.0), spacing=0.02),
                     flat_wall(length=4.0, height=2.4, spacing=0.02, y0=0.0)])
    write_binary_ply(pts, tmp_path / "room.ply")
    crit = load_criteria()["wall-kcs-tilt-other"]
    stats = analyze_wall(tmp_path / "room.ply", 1.0, crit, 8.0, tmp_path / "out")
    fr = stats["walls"][0]["frame"]
    assert set(fr) == {"p0", "direction", "normal", "u_min", "u_max", "z_min", "z_max"}
    assert len(fr["p0"]) == 2 and len(fr["direction"]) == 2 and len(fr["normal"]) == 2
```
(test_pipeline.py 상단에 `from flatness.core.pipeline import analyze_wall`이 없으면 추가)

- [ ] **Step 2: 실패 확인** — Run: `python -m pytest tests/test_walls.py::test_ceiling_band_excluded tests/test_pipeline.py::test_wall_error_isolated tests/test_pipeline.py::test_wall_frame_serialized -v` → 3 FAIL

- [ ] **Step 3: 구현**

`walls.py` project_wall_points 마스크: `& (v <= wall.z_max - edge_margin_m)` (docstring에 "천장 접합부도 동일 마진" 추가).
`pipeline.py` analyze_wall 벽 루프 본문을 try/except로:
```python
    for i, wall in enumerate(walls, 1):
        try:
            uvw = project_wall_points(iter_chunks(path, chunk_size=chunk_size),
                                      info, scale_to_m, wall)
            grid = wall_grid(uvw, subcell_m=subcell_m)
            if int(np.isfinite(grid.median_z).sum()) < 10:
                warns.add(f"wall_{i}_skipped")
                continue
            cells, grades, w, wm = evaluate_wall(grid, criterion, u_mm, cell_m=cell_m)
        except ValueError:
            warns.add(f"wall_{i}_skipped")  # 벽별 실패 격리(티켓 17): 성한 벽 결과는 보존
            continue
        ...
        walls_out.append({"wall_id": i, "n_cells": len(cells),
                          "frame": {"p0": [round(float(v), 4) for v in wall.p0],
                                    "direction": [round(float(v), 4) for v in wall.direction],
                                    "normal": [round(float(v), 4) for v in wall.normal],
                                    "u_min": round(wall.u_min, 4), "u_max": round(wall.u_max, 4),
                                    "z_min": round(wall.z_min, 4), "z_max": round(wall.z_max, 4)},
                          **wm})
```
(기존 "유효 서브셀 <10 continue"는 경고 추가로 흡수. 벽 높이 관련 기존 테스트의 height_m 기대(2.25→2.15 등)가 변하면 실측값으로 갱신하되 근거를 리포트에 기록 — 마진 대칭화로 인한 정당한 변화다)

- [ ] **Step 4: 통과 확인 + 전체 스위트** — Run: `python -m pytest tests/test_walls.py tests/test_pipeline.py -v && python -m pytest -q` → 99 예상

- [ ] **Step 5: Commit** — `fix(engine): 벽 하드닝 — 오류 격리·천장 마진·월드 프레임 직렬화 (티켓 17·18·19)`

---

### Task 2: 버전·메타 정리 + 텍스트 리더 방어 (티켓 21·15)

**Files:**
- Modify: `engine/flatness/__init__.py`, `engine/flatness/core/pipeline.py`, `engine/flatness/io/text_reader.py`
- Test: `engine/tests/test_pipeline.py`, `engine/tests/test_text_reader.py` (추가)

**Interfaces:**
- Produces: `flatness.ENGINE_VERSION = "p1d-0.4.0"` — analyze_floor/analyze_wall meta가 이 상수와 `surface`('floor'/'wall')를 사용. 텍스트 리더는 nan/inf 토큰 행 스킵

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_pipeline.py 에 추가
def test_meta_version_and_surface(tmp_path):
    from flatness import ENGINE_VERSION
    write_binary_ply(flat_floor(size=(3.0, 3.0), spacing=0.02), tmp_path / "f.ply")
    stats = analyze_floor(tmp_path / "f.ply", 1.0, CRIT, 5.0, tmp_path / "out")
    assert stats["meta"]["engine_version"] == ENGINE_VERSION
    assert stats["meta"]["surface"] == "floor"
```

```python
# engine/tests/test_text_reader.py 에 추가
def test_nonfinite_rows_skipped(tmp_path):
    # 티켓 15: nan/inf 토큰이 bbox를 오염시켜 무의미한 오류를 유발하는 것을 방지
    (tmp_path / "a.xyz").write_text("0 0 0\nnan 1 1\n1 inf 0\n2 2 0.01\n", encoding="utf-8")
    got = _collect(tmp_path / "a.xyz")
    assert got.shape == (2, 3)
```

- [ ] **Step 2: 실패 확인** — 2 FAIL

- [ ] **Step 3: 구현**

`flatness/__init__.py`: `ENGINE_VERSION = "p1d-0.4.0"  # 단일 버전 스탬프 — 파이프라인·임포터 공유`
`pipeline.py`: 두 meta의 `"engine_version"`을 `ENGINE_VERSION`으로, floor meta에 `"surface": "floor"` 추가 (import는 `from flatness import ENGINE_VERSION`).
`text_reader.py` — append 전에 유한성 검사(기존 try/append 구조를 아래로 교체, 파일 상단 `import math` 추가):
```python
            try:
                row = (float(parts[0]), float(parts[1]), float(parts[2]))
            except ValueError:
                continue  # 비수치 행(헤더 등)
            if not all(math.isfinite(v) for v in row):
                continue  # nan/inf 행 스킵 (티켓 15)
            buf.append(row)
```

- [ ] **Step 4: 통과 확인 + 전체 스위트** — 101 예상

- [ ] **Step 5: Commit** — `fix(engine): ENGINE_VERSION 단일화·meta.surface + 텍스트 리더 비유한값 방어 (티켓 21·15)`

---

### Task 3: 자동 종합의견 템플릿 (스펙 §5.3)

**Files:**
- Create: `engine/flatness/outputs/summary.py`
- Modify: `engine/flatness/core/pipeline.py` (stats["auto_summary"] 주입 — write_outputs 호출 전)
- Test: `engine/tests/test_summary.py`

**Interfaces:**
- Produces: `generate_summary(stats) -> str` — 한국어 다단락 문자열(cp949 안전). 구성: ①판정 분포 요약 ②최악 지점(값·좌표·구역/벽) ③경고 해석(warnings 코드→문구 매핑) ④종합 문구(최악 등급 기반) ⑤스크리닝 한계 고지("공식 검측(실물 직선자·레벨)을 대체하지 않습니다" 포함). 두 파이프라인이 `stats["auto_summary"]`로 주입(LLM 미사용)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_summary.py
from flatness.outputs.summary import generate_summary

def _base_stats(**over):
    s = {"n_cells": 30, "n_valid": 28,
         "grade_counts": {"pass": 20, "borderline": 5, "repair": 3, "rework": 0, "na": 2},
         "worst": {"value_mm": 12.3, "point_x": 2.1, "point_y": 1.4, "cell_ix": 2, "cell_iy": 1},
         "coverage_pct": 93.5, "warnings": [],
         "applied_criteria": {"name": "floor-kcs-exposed", "pass_mm": 7, "rework_mm": 21, "u_mm": 5.0},
         "meta": {"surface": "floor"}, "zones": [], "value_max_mm": 12.3}
    s.update(over)
    return s

def test_summary_core_sections():
    t = generate_summary(_base_stats())
    assert "적합 20" in t and "보수 3" in t
    assert "12.3" in t and "(2.1, 1.4)" in t
    assert "대체하지 않습니다" in t          # 스크리닝 고지 필수
    assert "보수" in t                        # 최악 등급 문구

def test_summary_warning_mapping():
    t = generate_summary(_base_stats(warnings=["ghost_layer_rescan", "low_coverage"]))
    assert "이중 표면" in t and "재스캔" in t
    assert "인식률" in t

def test_summary_all_pass():
    t = generate_summary(_base_stats(
        grade_counts={"pass": 28, "borderline": 0, "repair": 0, "rework": 0, "na": 2}))
    assert "기준을 만족" in t

def test_summary_cp949_safe():
    t = generate_summary(_base_stats(warnings=["ghost_layer_rescan", "furniture_excluded",
                                               "plumbness_relative_to_z", "low_coverage",
                                               "uncertainty_swallows_repair", "reduced_span",
                                               "wall_2_skipped", "ghost_zone_excluded"]))
    t.encode("cp949")  # 예외 없이 인코딩되어야 함
```

- [ ] **Step 2: 실패 확인** — FAIL (모듈 없음)

- [ ] **Step 3: 구현**

```python
# engine/flatness/outputs/summary.py
"""자동 종합의견 — stats 기반 규칙 템플릿 (스펙 §5.3, LLM 미사용·재현성)."""

_WARN_TEXT = {
    "ghost_layer_rescan": "일부 구역에서 이중 표면(유령층)이 감지되어 해당 지역은 판정에서 제외되었습니다. 재스캔을 권장합니다.",
    "ghost_zone_excluded": "이중 표면 비율이 높은 구역이 통째로 제외되었습니다. 재스캔을 권장합니다.",
    "furniture_excluded": "가구 상판으로 의심되는 면이 판정에서 제외되었습니다.",
    "low_coverage": "바닥 인식률이 낮습니다. 스캔 범위와 장애물을 확인하세요.",
    "plumbness_relative_to_z": "수직도는 스캔 좌표계의 z축 기준 상대 지표입니다(중력 보정 아님).",
    "uncertainty_swallows_repair": "측정 불확도가 보수 구간을 잠식하여 경계 판정이 확대되었습니다.",
    "reduced_span": "일부 구간은 공간 제약으로 축소 스팬(허용치 선형 환산)으로 판정되었습니다.",
}
_GRADE_LABEL = {"pass": "적합", "borderline": "경계", "repair": "보수", "rework": "재시공"}


def _worst_grade(gc):
    for g in ("rework", "repair", "borderline"):
        if gc.get(g, 0) > 0:
            return g
    return "pass"


def generate_summary(stats):
    gc = stats["grade_counts"]
    crit = stats["applied_criteria"]
    lines = []
    surface = "벽면" if stats.get("meta", {}).get("surface") == "wall" else "바닥면"
    lines.append(f"{surface} 평활도 분석 결과, 판정 셀 {stats['n_cells']}개(유효 {stats['n_valid']}개) 중 "
                 f"적합 {gc['pass']}개, 경계 {gc['borderline']}개, 보수 {gc['repair']}개, "
                 f"재시공 {gc['rework']}개, 판정 불가 {gc['na']}개로 나타났습니다. "
                 f"적용 기준은 {crit['name']}(허용 {crit['pass_mm']}mm, 불확도 U={crit['u_mm']}mm)입니다.")
    w = stats.get("worst")
    if w:
        lines.append(f"최대 편차는 {w['value_mm']}mm로 좌표 ({w['point_x']:.1f}, {w['point_y']:.1f}) 부근에서 "
                     f"관측되었습니다.")
    wg = _worst_grade(gc)
    if wg == "pass":
        lines.append("전 구간이 적용 기준을 만족합니다.")
    elif wg == "borderline":
        lines.append("일부 구간이 경계 판정입니다. 현장 재확인(실물 직선자 검측)을 권장합니다.")
    elif wg == "repair":
        lines.append("보수 검토 대상 구간이 존재합니다. 해당 위치의 보수 계획 수립을 권장합니다.")
    else:
        lines.append("재시공 검토 대상 구간이 존재합니다. 정밀 검측 및 시공 검토가 필요합니다.")
    for code in stats.get("warnings", []):
        if code in _WARN_TEXT:
            lines.append(_WARN_TEXT[code])
        elif code.startswith("wall_") and code.endswith("_skipped"):
            lines.append(f"벽 {code.split('_')[1]}번은 평가 불가로 제외되었습니다.")
    lines.append("본 결과는 모바일 LiDAR 기반 스크리닝이며 공식 검측(실물 직선자·레벨 측량)을 대체하지 않습니다.")
    return "\n".join(lines)
```
`pipeline.py`: 두 파이프라인에서 `write_outputs` 호출 **전**에 `stats["auto_summary"] = generate_summary(stats)` (import 추가).

- [ ] **Step 4: 통과 확인 + 전체 스위트** — 105 예상 (기존 pipeline 테스트가 stats 키 추가로 깨지지 않는지 확인)

- [ ] **Step 5: Commit** — `feat(engine): 자동 종합의견 규칙 템플릿 (스펙 §5.3)`

---

### Task 4: 3D 프리뷰 PNG (스펙 §5.1.9)

**Files:**
- Create: `engine/flatness/outputs/preview3d.py`
- Modify: `engine/flatness/core/pipeline.py` (analyze_floor에서 생성·경로 기록)
- Test: `engine/tests/test_preview3d.py`

**Interfaces:**
- Produces: `render_preview3d(residuals, grid, out_dir, worst_xy=None, max_points=50000) -> list[str]` — `preview3d.png`(전체 등각) + worst_xy가 있으면 `preview3d_zoom.png`(±1.5m 확대) 생성, 생성한 파일명 리스트 반환. analyze_floor가 stats["preview3d_paths"]에 기록. matplotlib Agg·한글 폰트는 heatmap 모듈 설정 재사용(먼저 import). 벽 프리뷰는 후속(백로그)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_preview3d.py
import numpy as np
from tests.fixtures.synthetic import flat_floor, add_bump
from flatness.outputs.preview3d import render_preview3d
from tests.test_subcell import _grid

def test_preview_files_created(tmp_path):
    pts = add_bump(flat_floor(size=(4.0, 3.0), spacing=0.02), (2.0, 1.5), 0.3, -0.01)
    g = _grid(pts)
    res = g.median_z - np.nanmedian(g.median_z)
    names = render_preview3d(res, g, tmp_path, worst_xy=(2.0, 1.5))
    assert names == ["preview3d.png", "preview3d_zoom.png"]
    assert (tmp_path / "preview3d.png").stat().st_size > 5000
    assert (tmp_path / "preview3d_zoom.png").stat().st_size > 5000

def test_preview_without_worst(tmp_path):
    g = _grid(flat_floor(size=(2.0, 2.0), spacing=0.02))
    res = g.median_z - np.nanmedian(g.median_z)
    names = render_preview3d(res, g, tmp_path)
    assert names == ["preview3d.png"]
```

- [ ] **Step 2: 실패 확인** — FAIL (모듈 없음)

- [ ] **Step 3: 구현**

```python
# engine/flatness/outputs/preview3d.py
"""3D 프리뷰 PNG — matplotlib 등각 산점도 2장(전체 + 최대 결함 확대), 서버측 정적 렌더.

헤드리스 WebGL은 사용하지 않는다(스펙 §8 결정). 폰트·Agg 설정은 heatmap 모듈이 담당.
"""
import numpy as np
from flatness.outputs import heatmap as _hm  # Agg·한글 폰트 설정 재사용(부수효과 import)
import matplotlib.pyplot as plt


def _scatter(ax, xs, ys, zs, title):
    p = ax.scatter(xs, ys, zs * 1000.0, c=zs * 1000.0, cmap="RdYlGn_r", s=2)
    ax.set_xlabel("X (m)")
    ax.set_ylabel("Y (m)")
    ax.set_zlabel("편차 (mm)")
    ax.set_title(title)
    return p


def render_preview3d(residuals, grid, out_dir, worst_xy=None, max_points=50000):
    ys, xs = np.nonzero(np.isfinite(residuals))
    if len(xs) == 0:
        return []
    cx = grid.origin[0] + (xs + 0.5) * grid.size_m
    cy = grid.origin[1] + (ys + 0.5) * grid.size_m
    zz = residuals[ys, xs].astype(float)
    if len(cx) > max_points:
        idx = np.random.default_rng(0).choice(len(cx), max_points, replace=False)
        cx, cy, zz = cx[idx], cy[idx], zz[idx]
    names = []
    fig = plt.figure(figsize=(8, 6))
    ax = fig.add_subplot(111, projection="3d")
    p = _scatter(ax, cx, cy, zz, "평활도 편차 3D 프리뷰")
    fig.colorbar(p, ax=ax, shrink=0.6, label="편차 (mm)")
    fig.tight_layout()
    fig.savefig(out_dir / "preview3d.png", dpi=110)
    plt.close(fig)
    names.append("preview3d.png")
    if worst_xy is not None:
        m = (np.abs(cx - worst_xy[0]) <= 1.5) & (np.abs(cy - worst_xy[1]) <= 1.5)
        if m.any():
            fig = plt.figure(figsize=(8, 6))
            ax = fig.add_subplot(111, projection="3d")
            p = _scatter(ax, cx[m], cy[m], zz[m], "최대 결함 구역 확대")
            fig.colorbar(p, ax=ax, shrink=0.6, label="편차 (mm)")
            fig.tight_layout()
            fig.savefig(out_dir / "preview3d_zoom.png", dpi=110)
            plt.close(fig)
            names.append("preview3d_zoom.png")
    return names
```
`pipeline.py` analyze_floor: `write_outputs` 전에
```python
    worst = stats.get("worst")
    stats["preview3d_paths"] = render_preview3d(
        residuals, grid, out_dir,
        worst_xy=None if worst is None else (worst["point_x"], worst["point_y"]))
```
(out_dir.mkdir가 앞서 없으면 `out_dir.mkdir(parents=True, exist_ok=True)`를 write 전에 보장 — write_outputs가 이미 mkdir하므로 순서상 render 전에 직접 한 번 호출)

- [ ] **Step 4: 통과 확인 + 전체 스위트** — 107 예상

- [ ] **Step 5: Commit** — `feat(engine): 3D 프리뷰 PNG (전체+최대결함 등각 뷰)`

---

### Task 5: 기존 결과 임포트 (스펙 §5.4)

**Files:**
- Create: `engine/flatness/importer/__init__.py`(빈 파일), `engine/flatness/importer/colab_csv.py`
- Modify: `engine/flatness/cli.py` (`import-colab` 서브커맨드)
- Test: `engine/tests/test_import_colab.py`

**Interfaces:**
- Produces: `import_colab_csv(path, criterion, u_mm, out_dir, subcell_m=0.05, cell_m=1.0) -> dict` — Colab CSV(`X,Y,Z,Distance_mm,Signed_Distance_mm,...` 헤더 필수)의 Signed_Distance_mm(이미 평면 제거된 편차)를 (X,Y) 서브셀 중앙값으로 비닝 → 직선자 셀 판정 → stats/산출물(히트맵·CSV·요약 포함). meta: `engine_version='external-colab-v1'`, `surface='floor'`, `source='colab-import'`. 필수 컬럼 부재 시 `ValueError("Colab CSV 형식 아님: X,Y,Signed_Distance_mm 컬럼 필요")`
- CLI: `flatness import-colab FILE --out DIR [--criteria NAME] [--uncertainty-mm F]` — analyze와 동일 exit 규약(단위 확인 불필요 — mm 명시 포맷)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# engine/tests/test_import_colab.py
import numpy as np
from flatness.importer.colab_csv import import_colab_csv
from flatness.criteria import load_criteria
import pytest

CRIT = load_criteria()["floor-kcs-exposed"]

def _write_csv(path, pts, signed_mm):
    with open(path, "w", encoding="utf-8", newline="\n") as f:
        f.write("X,Y,Z,Distance_mm,Signed_Distance_mm,R,G,B,Is_Uneven\n")
        for (x, y, z), s in zip(pts, signed_mm):
            f.write(f"{x},{y},{z},{abs(s)},{s},0,128,0,False\n")

def test_import_detects_depression(tmp_path):
    from tests.fixtures.synthetic import flat_floor
    pts = flat_floor(size=(6.0, 6.0), spacing=0.02)
    r = np.hypot(pts[:, 0] - 2.0, pts[:, 1] - 2.0)
    signed = np.where(r < 0.3, -10.0 * 0.5 * (1.0 + np.cos(np.pi * r / 0.3)), 0.0)
    _write_csv(tmp_path / "colab.csv", pts, signed)
    stats = import_colab_csv(tmp_path / "colab.csv", CRIT, 5.0, tmp_path / "out")
    assert 9.0 <= stats["worst"]["value_mm"] <= 11.0
    assert abs(stats["worst"]["point_x"] - 2.0) < 1.0
    assert stats["meta"]["engine_version"] == "external-colab-v1"
    assert stats["meta"]["source"] == "colab-import"
    assert (tmp_path / "out" / "heatmap.png").exists()
    assert "auto_summary" in stats

def test_import_rejects_wrong_schema(tmp_path):
    (tmp_path / "bad.csv").write_text("a,b,c\n1,2,3\n", encoding="utf-8")
    with pytest.raises(ValueError, match="Colab CSV"):
        import_colab_csv(tmp_path / "bad.csv", CRIT, 5.0, tmp_path / "out")

def test_import_cli(tmp_path):
    from tests.fixtures.synthetic import flat_floor
    from tests.test_cli import _run
    pts = flat_floor(size=(3.0, 3.0), spacing=0.02)
    _write_csv(tmp_path / "colab.csv", pts, np.zeros(len(pts)))
    r = _run("import-colab", str(tmp_path / "colab.csv"), "--out", str(tmp_path / "out"))
    assert r.returncode == 0, r.stderr
    assert "외부 결과" in r.stdout
```

- [ ] **Step 2: 실패 확인** — FAIL (모듈 없음)

- [ ] **Step 3: 구현**

```python
# engine/flatness/importer/colab_csv.py
"""기존 Colab 노트북 결과 CSV 임포트 (스펙 §5.4 — 과업지시서 '분석 결과 자동 불러오기').

Signed_Distance_mm는 기존 프로그램이 이미 평면 제거한 편차이므로 재분석 없이
서브셀 중앙값 → 직선자 셀 판정 기계에 직접 태운다.
"""
import csv as _csv
import numpy as np
from flatness import ENGINE_VERSION  # noqa: F401  (버전 대비 참조용)
from flatness.io.reader import CloudInfo
from flatness.core.subcell import build_subcell_grid
from flatness.core.cells import evaluate_cells
from flatness.criteria import grade_cells
from flatness.outputs.stats import build_stats, write_outputs
from flatness.outputs.heatmap import render_heatmap
from flatness.outputs.summary import generate_summary


def _load(path):
    with open(path, "r", encoding="utf-8-sig", newline="") as f:
        reader = _csv.DictReader(f)
        cols = reader.fieldnames or []
        need = {"X", "Y", "Signed_Distance_mm"}
        if not need.issubset(set(cols)):
            raise ValueError("Colab CSV 형식 아님: X,Y,Signed_Distance_mm 컬럼 필요")
        xs, ys, ss = [], [], []
        for row in reader:
            try:
                x, y, s = float(row["X"]), float(row["Y"]), float(row["Signed_Distance_mm"])
            except (ValueError, TypeError):
                continue
            if not (np.isfinite(x) and np.isfinite(y) and np.isfinite(s)):
                continue
            xs.append(x); ys.append(y); ss.append(s)
    if not xs:
        raise ValueError("Colab CSV에 유효 행 없음")
    return np.column_stack([xs, ys, np.asarray(ss) / 1000.0])  # 편차를 m로


def import_colab_csv(path, criterion, u_mm, out_dir, subcell_m=0.05, cell_m=1.0):
    pts = _load(path)
    info = CloudInfo(len(pts), pts.min(axis=0), pts.max(axis=0))
    grid = build_subcell_grid(iter([pts]), info, 1.0, subcell_m=subcell_m)
    if int(np.isfinite(grid.median_z).sum()) < 10:
        raise ValueError("유효 서브셀 부족: 임포트 데이터 확인 필요")
    residuals = (grid.median_z - np.nanmedian(grid.median_z)).astype(np.float32)
    span = criterion.span_m if criterion.span_m else 3.0
    cells = evaluate_cells(residuals, grid, span_m=span, cell_m=cell_m)
    grades, warns = grade_cells(cells, criterion, u_mm)
    meta = {"file": str(path), "n_points": len(pts), "engine_version": "external-colab-v1",
            "surface": "floor", "source": "colab-import", "subcell_m": subcell_m, "cell_m": cell_m}
    stats = build_stats(cells, grades, criterion, u_mm, sorted(set(warns)), meta)
    out_dir.mkdir(parents=True, exist_ok=True)
    stats["auto_summary"] = generate_summary(stats)
    write_outputs(out_dir, stats, cells, grades)
    render_heatmap(cells, grades, out_dir / "heatmap.png", cell_m=cell_m)
    return stats
```
`cli.py`: 서브커맨드 추가:
```python
    ic = sub.add_parser("import-colab", help="기존 Colab 결과 CSV 임포트")
    ic.add_argument("file", type=Path)
    ic.add_argument("--out", type=Path, required=True)
    ic.add_argument("--criteria", default="floor-kcs-exposed")
    ic.add_argument("--uncertainty-mm", type=float, default=5.0)
```
분기(list-criteria 처리 다음):
```python
    if args.cmd == "import-colab":
        crit = crits.get(args.criteria)
        if crit is None or crit.metric != "flatness":
            print(f"오류: 알 수 없는 기준 '{args.criteria}': flatness list-criteria 참고")
            return 1
        from flatness.importer.colab_csv import import_colab_csv
        try:
            stats = import_colab_csv(args.file, crit, args.uncertainty_mm, args.out)
        except (ValueError, OSError) as e:
            print(f"임포트 실패: {e}")
            return 1
        gc = stats["grade_counts"]
        print(f"외부 결과 임포트 완료: 셀 {stats['n_cells']}개 (유효 {stats['n_valid']})")
        print(f"  적합 {gc['pass']} / 경계 {gc['borderline']} / 보수 {gc['repair']}"
              f" / 재시공 {gc['rework']} / 판정불가 {gc['na']}")
        print(f"  산출물: {args.out}")
        return 0
```

- [ ] **Step 4: 통과 확인 + 전체 스위트** — 110 예상

- [ ] **Step 5: Commit** — `feat(engine): Colab 결과 CSV 임포트 + CLI import-colab (스펙 §5.4)`

---

### Task 6: 30M 점 성능·메모리 실측 (스펙 §5.2·§12)

**Files:**
- Modify: `engine/pyproject.toml` (dev deps에 `"psutil>=5.9"`, pytest addopts/markers)
- Create: `engine/tests/perf/__init__.py`(빈 파일), `engine/tests/perf/test_memory_spike.py`

**Interfaces:**
- Produces: `@pytest.mark.perf` 테스트 — 30M 점 binary PLY를 청크 스트리밍으로 생성(피크 메모리 오염 방지) → analyze_floor 실행 → 경과 시간 < 300s, 피크 RSS 증가 < 2GiB 단언 + 실측치 출력. 기본 스위트에서 제외

- [ ] **Step 1: pyproject 수정**

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-m 'not perf'"
markers = ["perf: 대용량 성능·메모리 실측 (기본 제외, -m perf 로 실행)"]
```
dev deps: `dev = ["pytest>=8", "psutil>=5.9"]` → `pip install -e ".[dev]"`

- [ ] **Step 2: 테스트 작성**

```python
# engine/tests/perf/test_memory_spike.py
"""30M 점 성능·메모리 실측 (스펙 §5.2: 1 vCPU/2GB에서 5분) — 기본 스위트 제외."""
import time
import numpy as np
import psutil
import pytest
from flatness.core.pipeline import analyze_floor
from flatness.criteria import load_criteria

N_POINTS = 30_000_000
SIZE = (30.0, 20.0)  # 600m² — 30M점이면 점간격 ≈ 4.5mm


def _write_big_ply(path):
    # 메모리 오염 없이 스트리밍 생성: 행 단위 절반씩 생성해 즉시 기록
    header = (f"ply\nformat binary_little_endian 1.0\nelement vertex {N_POINTS}\n"
              "property float x\nproperty float y\nproperty float z\nend_header\n")
    rng = np.random.default_rng(0)
    per_chunk = 2_000_000
    written = 0
    with open(path, "wb") as f:
        f.write(header.encode())
        while written < N_POINTS:
            k = min(per_chunk, N_POINTS - written)
            xs = rng.uniform(0, SIZE[0], k).astype(np.float32)
            ys = rng.uniform(0, SIZE[1], k).astype(np.float32)
            zs = rng.normal(0.0, 0.001, k).astype(np.float32)  # 1mm 노이즈 평탄 바닥
            f.write(np.column_stack([xs, ys, zs]).astype("<f4").tobytes())
            written += k


@pytest.mark.perf
def test_30m_points_within_budget(tmp_path):
    big = tmp_path / "big.ply"
    _write_big_ply(big)
    proc = psutil.Process()
    rss0 = proc.memory_info().rss
    t0 = time.monotonic()
    stats = analyze_floor(big, 1.0, load_criteria()["floor-kcs-exposed"], 5.0, tmp_path / "out")
    dt = time.monotonic() - t0
    rss_peak = proc.memory_info().rss  # 종료 시점 RSS(근사) — 피크 추적은 아래 참고
    print(f"\n[perf] 30M점: {dt:.1f}s, RSS 증가 {(rss_peak - rss0) / 2**30:.2f} GiB, "
          f"셀 {stats['n_cells']} 유효 {stats['n_valid']} coverage {stats['coverage_pct']}%")
    assert dt < 300.0
    assert (rss_peak - rss0) < 2 * 2**30
```
주의: RSS는 종료 시점 근사치다 — 정밀 피크가 필요하면 백그라운드 폴링 스레드로 max RSS를 추적하도록 확장하라(구현자가 추가해도 좋음, 30줄 이내). 실측치는 리포트에 반드시 기록.

- [ ] **Step 3: 실행** — `python -m pytest tests/perf -m perf -v -s` (수 분 소요). 예산 초과 시 값 증거와 BLOCKED(스펙 §5.2 fallback 결정 필요). 기본 스위트(`python -m pytest -q`)가 여전히 110개로 도는지 확인(perf 제외 동작 검증)

- [ ] **Step 4: Commit** — `test(engine): 30M 점 성능·메모리 실측 perf 테스트 (기본 제외)`

---

### Task 7: 스모크 + 백로그 표기

- [ ] **Step 1:** `python -m pytest -q` 전체 확인 (110), `python -m pytest tests/perf -m perf` 실측치 재확인(Task 6에서 이미 실행했으면 결과 인용)
- [ ] **Step 2:** 임포트 스모크: Colab CSV 데모 생성(결함 주입) → `flatness import-colab ... --out demo_ic_out` → exit 0·"외부 결과" 출력·히트맵 확인 후 정리(커밋 금지)
- [ ] **Step 3:** `docs/superpowers/plans/2026-07-28-p1b-backlog-notes.md` 티켓 15·17·18·19·21 앞에 `[해결: P1d]` 표기, 티켓 22~23은 유지. 커밋 `docs: P1d 해결 티켓 표기`
